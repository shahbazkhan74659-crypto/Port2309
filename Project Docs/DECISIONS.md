# Technical Decisions

These decisions were made during a prior planning discussion, before any production code was written. They are recorded here as established direction for `ARCHITECTURE.md` and `PHASES.md`. Where original reasoning was not captured at the time, this is marked explicitly rather than guessed.

## Decision: Production stack — Django + Django templates + Tailwind CSS + React/JS + relational database

- Status: Accepted
- Date: 2026-08-19 (initial stack decision); reconfirmed and extended 2026-08-24 (database layer added)
- Context: The project needed a technology stack for the production portfolio site, chosen before any implementation began. The 2026-08-19 decision covered the application/rendering layer but did not name a persistence layer; the owner confirmed the full stack including the database on 2026-08-24.
- Decision: Python Django with Django templates for server-rendered pages, styled with Tailwind CSS, with React + JavaScript for interactive pieces (per the 2026-08-19 discussion, mounted as islands into specific DOM nodes rather than a full single-page application), backed by a relational database — either PostgreSQL or MySQL. The owner explicitly deferred choosing between Postgres and MySQL until closer to a hosting decision (see below); the stack is also explicitly open to adding further tools as needed rather than being considered closed.
- Reasoning: To be defined for the app/rendering layer (not captured in the original planning record). For the DB engine specifically: deferred on purpose because Django's ORM makes switching low-cost early on, so the choice is better tied to the eventual hosting provider.
- Consequences: Production implementation has not yet started, so no consequences have been observed. `ARCHITECTURE.md`'s planned data/persistence section marks the DB engine as "To be defined — Postgres or MySQL." Note that the existing visual prototype (`prototype/index.html`) does not use this stack — it is a separate, framework-free artifact. See "Standalone static prototype" decision below.

## Decision: Three Django apps — `core`, `contents`, `hiring`

- Status: **Superseded** (2026-08-24) — see "Two Django apps — `core`, `projects`" below.
- Date: 2026-08-19
- Context: The site's feature set (Projects, Designs, Blog, Resume, Timeline, Hire Me, home/nav, search) needed to be organized into Django apps.
- Decision: Exactly three apps:
  - `core` — home/nav views + urls, and search bar logic
  - `contents` — Projects, Designs, Blog, Resume, and a vertical Timeline (college → now), as separate models within one app
  - `hiring` — the "Hire Me" feature logic
- Reasoning: To be defined beyond the boundary itself. (One nuance is recorded — see "Designs kept removable" below — but the broader rationale for exactly this three-way split was not captured.)
- Consequences: Never implemented, so nothing needs unwinding in code. Superseded by the owner's explicit simplification to two apps.

## Decision: Two Django apps — `core`, `projects`

- Status: Accepted
- Date: 2026-08-24
- Context: While naming Phase 4 of the roadmap, the owner explicitly discarded the earlier three-app plan (`core`/`contents`/`hiring`) in favor of a simpler two-app structure.
- Decision: Exactly two apps:
  - `core` — views, URLs, and the main routing of the site; identity (e.g. About/home identity content), etc.
  - `projects` — the owner's projects, works, and code — replaces the "Projects" portion of the old `contents` app.
- Reasoning: Owner's explicit direction: "forget those three apps we decided back then... only two apps... that's enough."
- Consequences: Where Designs, Blog, Resume, Timeline, and the "Hire Me" feature (previously slated for `contents`/`hiring`) now live was initially an open question — **resolved 2026-08-24, see "Feature placement: Designs/Blog/Resume/Timeline/Hire Me → `core`" below.** The "Designs kept removable via feature flag" decision below also needed revisiting once this was resolved, since it was written against the now-superseded `contents` app — see that decision's updated Consequences.

## Decision: Global `templates/` and `static/` directories, not per-app

- Status: Accepted
- Date: 2026-08-19
- Context: Django supports both a per-app `templates/`/`static/` layout and a single global layout at the project root. Per-app namespacing was initially considered.
- Decision: One global `templates/` folder and one global `static/` folder at the project root.
- Reasoning: The project owner was explicit about preferring this after initially considering per-app namespacing. The detailed rationale beyond stated preference: To be defined.
- Consequences: Do not re-suggest per-app template/static namespacing for this project.

## Decision: Designs feature kept removable via feature flag

- Status: Accepted in principle, but **app placement pending** (2026-08-24) — see the `core`/`projects` decision above.
- Date: 2026-08-19
- Context: The Designs feature may be dropped from the site in the future, unlike Projects, Blog, Resume, and Timeline.
- Decision: Implement Designs as its own model with a feature flag gating its URLs, nav entry, and search visibility, kept isolated so it can be removed without disturbing Projects, Blog, Resume, or Timeline.
- Reasoning: Anticipated possibility that Designs will be cut; isolating it avoids entangling its removal with unrelated features.
- Consequences: The isolation principle (own model + feature flag) still stands, but it originally assumed a `contents` app that no longer exists in the plan. As of 2026-08-24, Designs is placed in `core` (see "Feature placement: Designs/Blog/Resume/Timeline/Hire Me → `core`" below) — the feature-flag isolation still applies within `core`.

## Decision: Feature placement — Designs, Blog, Resume, Timeline, Hire Me → `core`

- Status: Accepted for Designs/Resume/Timeline/Hire Me; partially superseded for Blog (2026-08-26) — see "Blog `Post`/`Tag` relocated to `projects`; every tag-shaped table across `core`/`projects` converted to a shared `Tag` model" below.
- Date: 2026-08-24
- Context: The "Two Django apps — `core`, `projects`" decision above left it unresolved where Designs, Blog, Resume, Timeline, and Hire Me would live, deferring the choice per-feature. The owner has now settled it for all five at once rather than one at a time.
- Decision: Designs, Blog, Resume, Timeline, and Hire Me all live in the `core` app. `projects` is scoped strictly to the owner's projects/works/code — nothing else.
- Reasoning: Owner's explicit direction.
- Consequences: Phases 7 (Blog and Resume) and 8 (Timeline, Contact, Hire Me) now have a settled app target — see `PHASES.md`. Designs still has no phase assigned yet, but when it is scheduled it belongs in `core`. `TASKS.md`'s open-question entry on this is resolved.

## Decision: Build a standalone static visual prototype before production implementation

- Status: Accepted (observed via existing code)
- Date: Unknown exact date; file present in the repository as of 2026-08-23.
- Context: `prototype/index.html` exists as a single, self-contained, framework-free HTML/CSS/JS file implementing a client-side hash-routed mockup (Home, Projects list/detail, About, Contact) with placeholder persona ("Renzo Malik") and placeholder project content — built independently of the planned Django + Tailwind + React stack.
- Observed implementation: See `ARCHITECTURE.md` for full technical detail.
- Original reasoning: To be defined. (No record explains why a standalone vanilla-JS prototype was chosen over prototyping directly within the planned Django + Tailwind stack.)
- Consequences: None outstanding — see the "Prototype is a styling/design reference only" decision below, which resolves how it relates to the production build.

## Decision: Tailwind wired via npm + Tailwind CLI v4 (CSS-first `@theme` config)

- Status: Accepted
- Date: 2026-08-24 (Phase 2)
- Context: Phase 2 required a working Tailwind CSS build, per the confirmed stack. Node v24.18.0/npm 11.16.0 were confirmed available on the dev machine. Tailwind v4 supports CSS-first configuration (`@theme` in the stylesheet itself) instead of a separate `tailwind.config.js`.
- Decision: `package.json` at repo root with `tailwindcss` + `@tailwindcss/cli` as devDependencies. Source stylesheet at `static_src/css/input.css` declares the prototype's exact design tokens (colors, fonts) in an `@theme` block, plus the prototype's hand-written component CSS (nav, hero, sections, footer, etc.) ported near-verbatim rather than rewritten as Tailwind utility classes. Built to `static/css/main.css` via `npm run build:css` (one-shot) / `npm run watch:css` (dev). Build output is gitignored, like `node_modules/`.
- Reasoning: The prototype's design is bespoke art direction (fluid `clamp()` typography, custom color tokens, specific component layouts) rather than a Tailwind-utility-shaped design — porting the CSS as-is guarantees visual fidelity to the prototype (the owner's explicit priority for Phase 2) while still getting Tailwind's build pipeline in place for future utility-based/React-island work.
- Consequences: `main.css` is a build artifact, not checked into git — `npm install && npm run build:css` (or an already-built `static/css/main.css`) is required before `runserver` shows a styled page. As more Tailwind utilities get used going forward, this file will start mixing hand-written component CSS with generated utilities.

## Decision: Phase 2/3 static pages routed directly via `config/`, not an app

- Status: Accepted
- Date: 2026-08-24 (Phase 2)
- Context: Phase 2's Home page needed a working URL + view, but the `core`/`projects` apps aren't scaffolded until Phase 4 (see the "Two Django apps" decision above). Django doesn't require an app for basic template-rendering routes.
- Decision: `config/views.py` holds a plain `home` view; `config/urls.py` routes `path('', views.home, name='home')` directly, with no app involved. Phase 3's additional static pages are expected to follow the same pattern until Phase 4.
- Reasoning: Avoids scaffolding `core`/`projects` prematurely (out of Phase 2/3 scope) while still letting Home be a real, server-routed page rather than a static file.
- Consequences: This routing is expected to move into `core`'s `urls.py`/`views.py` once that app exists in Phase 4 — treat `config/views.py`'s page-rendering views as temporary, not a long-term pattern. **Done as of Phase 4, 2026-08-24** — see the `core.urls` decision below.

## Decision: `core.urls` as `ROOT_URLCONF`; `config/` holds only settings/WSGI/ASGI

- Status: Accepted
- Date: 2026-08-24 (Phase 4)
- Context: Phase 4 scaffolded `core` and `projects` and moved all routing out of `config/`, per the owner's explicit instruction that `config/` should end up containing only `settings.py`, `wsgi.py`, and `asgi.py` — no `urls.py`, no `views.py`. Django's `ROOT_URLCONF` setting must point at some module with a `urlpatterns` list, so removing `config/urls.py` entirely meant that module had to become something else.
- Decision: `ROOT_URLCONF = 'core.urls'` in `config/settings.py`. `core/urls.py` holds the full `urlpatterns` list, including `path('admin/', admin.site.urls)` — `core/views.py` holds all page views (`home`, `about`, `contact`, `projects`, `github`), moved verbatim from `config/`. It is included unnamespaced (there is no `include()` layer at all — `core.urls` *is* the root), so every existing `{% url 'home' %}`/`'about'`/`'contact'`/`'projects'`/`'github'` template reference kept working with zero template changes.
- Reasoning: This matches `core`'s own documented job description (`ARCHITECTURE.md`/the "Two Django apps" decision above: *"views, URLs, and the main routing of the site"*) — `core` acting as the actual root router isn't a workaround, it's what the app boundary was defined to do.
- Consequences: Any future app-specific urls (e.g. a `projects/urls.py`, once that app grows beyond empty) would be `include()`d from `core/urls.py`, not from a project-level `config/urls.py` — there isn't one. `core` permanently doubles as the site's top-level router. If a future phase ever needs project-level URL concerns (e.g. i18n URL prefixing, a maintenance-mode toggle) that don't belong in `core`, that would need revisiting this decision rather than silently reintroducing `config/urls.py`.

## Decision: Generic placeholder content for pre-content-model pages

- Status: Accepted
- Date: 2026-08-24 (Phase 2)
- Context: The prototype's text (name "Renzo Malik," bio, projects, socials) is fictional and, per `CLAUDE.md`, must not be reused as real content. The owner's real bio/project/contact content does not exist yet, and no content models exist until later phases.
- Decision: Pages built before real content or content models exist (starting with the Phase 2 Home page) use clearly generic, non-identity placeholder copy (e.g. "YOUR NAME," "ROLE / ROLE / ROLE," "Placeholder paragraph about you") hardcoded directly in the template — not the prototype's persona, and not passed via view context yet.
- Reasoning: Owner's explicit direction — real content decision deferred rather than fabricated.
- Consequences: Every page built under this decision will need its placeholder copy revisited once real content or content-backed models exist. Do not treat any placeholder string as real biographical/project data. **Partially revisited in Phase 5** — see "Real content replaces placeholders" below; the Projects page and Home's featured-project card are the deliberate remaining exception until model-backed content exists (Phase 4's `projects` app is scaffolded but still empty).

## Decision: Real content replaces placeholders (Phase 5); Projects/featured-project stay placeholder by choice

- Status: Accepted
- Date: 2026-08-24 (Phase 5)
- Context: The owner's real content (name, bio, skills, contact links, GitHub username) became available and was supplied for Phase 5. The owner explicitly scoped this phase narrower than `PHASES.md`'s original speculative Phase 5 description (real navigation/animation) — see the updated `PHASES.md` Phase 5 entry.
- Decision: Real content now replaces the generic placeholders in `base.html`, `home.html` (except the featured-project card), `about.html`, and `contact.html`. The site brand mark ("S.") is auto-derived from the real first name for now — the owner has a custom badge image to swap in as a separate, explicitly deferred follow-up (they asked to be consulted again before it's added). Home's featured-project card and the entire Projects page (`templates/pages/projects.html`) were explicitly excluded from this content swap by the owner and remain on mockup/placeholder content — not an oversight.
- Reasoning: Owner's explicit direction, given directly in response to a planning-time request for content ("credentials"). Keeping Projects/featured-project on placeholder content avoids describing specific real projects twice (once now as static text, again later as real `Project` model instances in a future phase) — better to wait for the model.
- Consequences: The About-snapshot headline (`ADJECTIVE.` × 3 placeholder) received real single-word content ("CURIOSITY." / "CREATION." / "EXPERIMENTATION.") rather than the owner's original full-sentence answer, which would have overflowed the large display-heading font sizing — the full sentences were kept as a new smaller supporting line (`.about-headline-sub`, new CSS class) underneath instead, per the owner's explicit choice between the two options offered. Any future work on Home's featured project or the Projects page should treat their current placeholder content as still fully "Phase 3 mockup," not real data to preserve.

## Decision: GitHub page fetches live data from the public API (Phase 5)

- Status: Accepted
- Date: 2026-08-24 (Phase 5)
- Context: The `/github/` page (added post-Phase 3) had shown static placeholder profile data with live-fetching left as an explicitly open, unscheduled `TASKS.md` item. The owner chose to implement it now as part of Phase 5, when asked directly.
- Decision: `core/views.py`'s `github` view calls `https://api.github.com/users/shahbazkhan74659-crypto` server-side (5s timeout, wrapped in `try/except requests.RequestException`), passing avatar/name/bio/repo/follower/following counts and the real profile URL into the template context. On any failure, it falls back to hardcoded real static values (not the old generic placeholder) so the page never 500s. `requests` (and its transitive deps: `certifi`, `charset-normalizer`, `idna`, `urllib3`) was added to `requirements.txt`, pinned via `pip freeze`.
- Reasoning: Owner's explicit choice between "live fetch now" and "static real values only" when asked during planning — live fetch keeps the stats always current with minimal extra implementation cost (one HTTP call).
- Consequences: GitHub's unauthenticated API caps out at 60 requests/hour per IP — fine for a low-traffic personal portfolio, but there is no caching yet. A future enhancement could add Django's cache framework if traffic ever approaches that limit; not needed now. The username is a hardcoded constant in `core/views.py` (`GITHUB_USERNAME`) since no settings/config model exists to store it dynamically.

## Decision: GitHub page also lists real repositories in a scrollable panel

- Status: Accepted
- Date: 2026-08-24 (post-Phase 5 addition, not part of any numbered phase)
- Context: The owner asked to also list their real GitHub repositories on `/github/`, read-only, each linking straight out to the repo on GitHub — same pattern as the profile-card fetch, using a hand-drawn mockup to show placement (beside the profile card, its own scrollable section).
- Decision: `core/views.py`'s `github` view makes a second call to `https://api.github.com/users/<username>/repos?sort=updated&per_page=100` (same timeout/error-handling pattern as the profile fetch — falls back to an empty list, not a crash), passing name/description/language/star-count/URL per repo. `templates/pages/github.html` renders them in a new `.repo-list` panel next to `.profile-card` (`.github-layout`, a two-column CSS grid — `grid-template-columns: 1fr 1fr; align-items: stretch;` — so both panels share equal width and height, revised same day after the owner flagged the initial flex layout as mismatched), each repo a full-row link (`.repo-item`) to its real GitHub URL; the panel scrolls internally (fixed `height: 460px; overflow-y: auto`, matching `.profile-card`'s height) rather than growing the page.
- Reasoning: Owner's explicit direction; sorted by most-recently-updated with no cap/filtering (all repos shown, forks included) since the owner didn't ask for either.
- Consequences: Same 60 req/hour unauthenticated rate-limit and no-caching situation as the profile fetch now applies twice per page load (two API calls) — still fine for current traffic, same future-caching note applies. If a repo's `description` is null, only the name/language/star row renders (no empty paragraph).

## Decision: Real "SK" badge image replaces the auto-derived text brand mark

- Status: Accepted
- Date: 2026-08-24 (post-Phase 5 addition, not part of any numbered phase)
- Context: The owner supplied a custom "SK" badge graphic (brushed-metal embossed plate, AI-generated) on a flat gray background with a soft drop shadow and a bright top bevel highlight — this was deliberately deferred back in Phase 5 (see above) with the owner asking to be consulted again before it was added. The owner then confirmed to proceed.
- Decision: Background removed via an edge-based flood fill (not the simple luminance-threshold technique used for the GitHub logo) — the badge's dark drop shadow and bright highlight rim meant no single brightness cutoff could separate it from the flat gray background cleanly, so pixels were grown outward from the image border using per-step luminance-difference tolerance (stopping at sharp edges regardless of whether they went darker or brighter), then only the largest connected foreground region was kept (dropping the small watermark sparkle and stray noise as disconnected islands) — cropped to `static/images/sk-badge.png`. Wired into `templates/base.html` as `.nav-mark-badge` (28px tall) and `.foot-mark-badge` (22px tall) images inside the existing `.nav-mark`/`.foot-mark` links/spans, replacing the "S." text.
- Reasoning: Owner's explicit direction; the two-stage technique (tolerance flood fill + largest-component filter) was necessary because this asset's background/shadow/highlight interplay made the GitHub-logo approach (recolor by luminance, single threshold) unsuitable — verified in-browser at both nav and footer sizes before considering it done.
- Consequences: The badge PNG is a photographic/textured asset (brushed metal, kept in its original shading), unlike the GitHub logo which was recolored flat white — future edits to the nav/footer mark should treat it as a fixed image asset, not something restyleable via CSS `color`.

## Decision: Timeline cut from the roadmap; Phase 6 merged into a redefined Phase 8

- Status: Accepted
- Date: 2026-08-24
- Context: Phase 6 ("Connecting Pages to the Apps, and Modals") was put on hold earlier the same day since no confirmed need for modal UI existed yet. Phase 8 was originally "Vertical Growth Timeline, Contact, and Hire Me." Revisiting Phase 8, the owner decided: the vertical Timeline feature isn't needed at all; Contact is already sufficiently covered by the real Contact page/info shipped in Phases 3 and 5; and Phase 6's held-back scope (a real `Project` model + a modal mechanism for viewing project details) should become Phase 8's actual content instead.
- Decision: Phase 8 is redefined as "Projects App: Modal & Mechanism, and Hire Me" — building the real `projects` app (a `Project` model, replacing the Phase 3/5 mockup content on the Projects page and Home's featured-project card) with a modal-based mechanism for viewing project details, plus the "Hire Me" feature. Phase 6 is marked superseded/merged into Phase 8 rather than left as a separate on-hold phase — its number and history stay in `PHASES.md` (not deleted), but its remaining work now lives under Phase 8. Timeline is removed from `PROJECT.md`'s Core Features list entirely — not deferred, cut. Contact gets no further scheduled work; the "working, backend-processed contact form" idea from the original Phase 8 description is not carried forward (the static Contact page with real email/GitHub/LinkedIn is considered sufficient) unless the owner raises it again later.
- Reasoning: Owner's explicit direction. Folding Phase 6's modal mechanism into Phase 8 avoids two phases doing overlapping "wire Projects up to real data" work, and gives the previously-speculative "modal UI" (see Phase 6's original scope) a concrete, confirmed purpose: project detail modals are exactly the interactivity Phase 6 was guessing at.
- Consequences: `PHASES.md`'s Phase 6 entry keeps its original objective/scope text as a historical record but its status changes to "Superseded — merged into Phase 8." Phase 7 (Blog and Resume) is unaffected and stays the immediate next phase before Phase 8. If Timeline or a working Contact form are ever wanted again, they would need a fresh phase-scope decision — they are not implicitly slotted into any existing phase anymore.

## Decision: Hero portrait redesigned as a large overlapping image, diverging from the prototype

- Status: Accepted
- Date: 2026-08-24 (Phase 2, post-completion refinement)
- Context: The Phase 2 Home page initially ported the prototype's hero portrait treatment as-is: a small (`clamp(150–300px)` wide) image floated right, with text wrapping around it via `shape-outside`. After adding the real portrait photo, the owner asked for a materially different treatment: an image spanning roughly half the screen, sized to its actual aspect ratio (head-to-stomach fully visible, no cropping), positioned top-right just below the sticky nav (slightly overlapping under it), with the hero text (`I BUILD.` / `I WRITE.` / name / `I EXPLORE.`) layered on top of the image via `z-index` rather than flowing around it.
- Decision: `.hero-portrait-slot` uses `position: absolute` sized by `height` + `aspect-ratio: 1149 / 1369` (the photo's real ratio) instead of the prototype's fixed float box; hero text gets `position: relative; z-index: 2` plus a soft `text-shadow` to stay legible over the photo.
- Reasoning: Owner's explicit direction, given after seeing the ported-as-is version rendered — the float/wrap treatment worked for the prototype's small decorative portrait but not for a large, real photo meant to anchor the hero.
- Consequences: This is an intentional, confirmed deviation from "match the prototype's structure" for this one element — do not "fix" it back toward the prototype's float-based layout. Other Home sections (featured project, statement, about snapshot, CTA, nav/footer) still follow the prototype's structure as recorded in the Phase 2 completion note in `PHASES.md`.

## Decision: GitHub gets a custom in-site profile page; LinkedIn/Email link out directly

- Status: Accepted (page built as a placeholder — live data fetch not yet implemented)
- Date: 2026-08-24 (post-Phase 3 addition, not part of any numbered phase)
- Context: The owner wanted a richer way to surface GitHub, LinkedIn, and Email beyond a bare link. Investigated feasibility for each: GitHub has a public REST API (`api.github.com/users/<username>`) usable without auth for basic profile data, so a live-fetched read-only card is realistic. LinkedIn has no public API for reading arbitrary profile data, and scraping their pages would violate their Terms of Service — a live "fetch my LinkedIn profile" feature isn't viable. Email has no profile to fetch beyond an optional third-party service (Gravatar), which wasn't pursued.
- Decision: Only GitHub gets a dedicated in-site page (`/github/`, `templates/pages/github.html`, new `.profile-card`/`.profile-*` CSS in `input.css`) — for now it renders placeholder content (generic name/bio/stats, no live API call), with live-fetching deferred to later. LinkedIn and Email get no custom UI — wherever they appear (Contact page, Home CTA), they stay plain outbound links. The site footer was trimmed to drop LinkedIn and Email entirely (they're only reachable via the Contact page's list now); the footer's GitHub link now points to the new in-site page instead of out to GitHub directly.
- Reasoning: Owner's explicit direction, given after discussing the technical/legal feasibility of each platform. Treated as a small, ad-hoc addition (one view + one template, no models or apps needed) rather than a new numbered phase — per `CLAUDE.md`'s rule that phase count/order is the owner's call, and this was scoped the same way the Phase 2 hero-portrait addition was (see the decision above).
- Consequences: The GitHub page currently shows placeholder data only (`&mdash;` stat counts, generic name/bio) with a `.form-note`-style disclaimer that live data isn't connected yet — a future addition should replace this with a real `requests.get()` call to GitHub's API (server-side, cached) once the owner wants that built. If LinkedIn or Email ever get their own API-backed integration, that would need a fresh feasibility discussion — the "no custom UI" call here was specifically because no viable public data source exists for them today.

## Decision: Home "See My Resume" button restyled as a bespoke neon-green pill (`.btn-neon`)

- Status: Accepted
- Date: 2026-08-25
- Context: The owner supplied a reference image (an AI-generated sci-fi/cyberpunk pill button — double neon-green ring with dashed "data segment" texture, glowing cyan-green text and arrow, on a matrix-rain/circuit backdrop) and asked for the Home hero's "See My Resume" button (added earlier this phase, originally `.btn-ghost` styled like every other button on the site) to be redesigned to match it, done in CSS if feasible rather than as an image asset.
- Decision: Built as a new `.btn-neon` CSS component (`static_src/css/input.css`) — a dark pill (`#030a06` fill) with a `2px solid #39ff8a` glowing border (multi-layer `box-shadow` for the outward glow), a `::before` inset ring for the double-border look, a `::after` `repeating-linear-gradient` mask on the top/bottom edges for the dashed "data segment" texture, and glowing `text-shadow` on the label/arrow — hover intensifies the glow and nudges the arrow, matching the site's existing hover-affordance convention (`.explore-link`, `.btn-line`). The matrix-rain/circuit/hexagon backdrop from the reference image was treated as demo staging, not part of the button itself, and was not reproduced. The color is neon green exactly as shown in the reference (owner's explicit choice when asked, over adapting it to the site's existing purple `--accent`) — this is a deliberate one-off departure from the site's otherwise-consistent purple accent, scoped to this single button only.
- Reasoning: Owner's explicit direction. CSS proved fully capable of reproducing the reference's core look (glowing double-ring pill, dashed texture, glowing text) without needing an image asset, so the "if possible with Tailwind, else image" fallback wasn't needed.
- Consequences: `.btn-neon` is the only green element on an otherwise purple-accented site — intentional, not an inconsistency to "fix" later. If more buttons want this treatment, `.btn-neon` can be reused directly; if the site's accent ever changes, this button's green is not tied to `--accent` and would need updating separately. Two implementation pitfalls worth remembering: (1) `.btn-neon` must not set its own `position` — `.hero-resume-btn` (or whatever positions the element) needs to own `position: absolute`, and since both are single-class selectors, whichever rule appears later in the stylesheet wins the cascade regardless of which one is "more specific" in intent. (2) `.hero-resume-btn` needs a `z-index` strictly higher than `.hero-line`/`.hero-name-row`'s `z-index: 2` (now `z-index: 3`) — at equal z-index, the full-width `<h1 class="hero-line">` block (later in DOM order) wins the stacking tie and silently intercepts hover/click over the button's area even though the button is visually on top, despite the two not visually overlapping (the h1's invisible box extends past its text).

## Decision: New Phase 11 "Seeding Real Projects" added; former Phase 11/12 renumbered to 12/13

- Status: Accepted
- Date: 2026-08-25
- Context: The build roadmap (Phase 0 through the original Phase 12) had been confirmed 2026-08-24 as the complete set, with no further phases expected. The owner identified a gap: Phase 8 builds the real `Project` model/views/modal mechanism, but nothing on the roadmap covered actually populating it with the owner's real project data (as opposed to the mockup/placeholder content Projects has carried since Phase 3/5) before Testing (old Phase 11) and Deployment (old Phase 12).
- Decision: Insert a new Phase 11, "Seeding Real Projects," positioned after Phase 10 (production database chosen) and before the (renumbered) Testing/Deployment phases — data population is scoped separately from Phase 8's model-building work. The former Phase 11 "Full End-to-End Testing" becomes Phase 12; the former Phase 12 "Deployment and Hosting" becomes Phase 13.
- Reasoning: Owner's explicit direction — added directly in `PHASES.md`. Placing it after Phase 10 (rather than immediately after Phase 8) means real project data is seeded once the production database is actually in place, not into a throwaway dev database.
- Consequences: `PHASES.md`'s intro note and closing "last phase" line were updated to reflect Phase 0–13 instead of Phase 0–12. This is a live example of `CLAUDE.md`'s "if new phases are identified later, add them here rather than assuming the roadmap is fixed forever" guidance — the roadmap is not permanently closed just because it was once confirmed complete.

## Decision: Blog and Resume ship as static placeholder pages (Phase 7); Resume PDF deferred

- Status: Accepted
- Date: 2026-08-25 (Phase 7)
- Context: Phase 7's scope ("Blog and Resume") had been left "to be defined" in `PHASES.md`. Neither real blog posts nor real resume experience/education content exist yet, and no `Post`/resume model has been built. The owner has a real resume PDF but wants to hold it back until the page "turns dynamic" in a future phase.
- Decision: `/blog/` ships as a static placeholder page — three hardcoded placeholder post entries, no model, mirroring the Phase 3 placeholder-Projects-page precedent (see "Real content replaces placeholders" decision below). `/resume/` ships as a styled page with a real Skills section (reusing the About page's real skill data verbatim — not fabricated) plus clearly-placeholder Experience and Education entries, and a "Download PDF" button wired to a static path (`static/files/resume.pdf`) that does not yet exist on disk — it will 404 until the owner supplies the real file later. Both routed via `core/views.py`/`core/urls.py`, same plain-`render` pattern as `about`/`contact`/`projects`. A footer "Blog" link was added (`templates/base.html`), and a "See My Resume" button was added to Home's hero section, positioned beside the left edge of the hero portrait — absolutely positioned via a new `--portrait-h` CSS custom property on `.hero` (shared with `.hero-portrait-slot`) so the button's vertical-center and horizontal offset stay derived from the portrait's own size, with a static/centered fallback under the existing 900px mobile breakpoint.
- Reasoning: Owner's explicit direction, given when asked directly during planning — real blog/resume content and the actual PDF are real biographical data that must not be fabricated (per the project's established "don't invent real content" convention), so placeholder structure now, real content later was the deliberate choice, same tradeoff Phase 3 made for Projects.
- Consequences: `/blog/` will need a real `Post` model (or similar) before it can hold real writing — not scheduled to any phase yet. `/resume/`'s Experience/Education sections and the PDF download need the owner's real content/file before they're genuinely useful — the owner has confirmed the PDF specifically waits until this page becomes dynamic. Do not treat any placeholder string on either page as real data.

## Decision: Phase 8 project-detail modal built as vanilla JS/CSS, not a React island

- Status: Accepted
- Date: 2026-08-25 (Phase 8)
- Context: `PHASES.md`'s Phase 8 scope flagged the project-detail modal as "likely where React/JS islands ... actually get used for the first time," per the production stack decision. No React tooling (bundler, build step) exists in the project yet. The owner was asked directly whether to set up React now for this one modal, or build it with the same no-framework approach the site has used everywhere else so far.
- Decision: The modal is a small vanilla-JS `<script>` block (in `templates/pages/projects.html`'s `extra_body` block, same IIFE/`"use strict"` convention as `prototype/index.html`) that clones a per-project `<template>` element into a shared `#project-modal-panel` and toggles visibility — no fetch, no framework. React islands remain fully deferred to a future phase.
- Reasoning: Owner's explicit choice, given directly when asked — avoids introducing a new build-tooling dependency (bundler/dev server) for a single, self-contained interaction that vanilla JS handles adequately.
- Consequences: React/JS islands are still unimplemented anywhere in the project — the "production stack" entry above remains partially aspirational. The real `/projects/<slug>/` detail page (not just the modal) is the actual no-JS/shareable-link destination, so the modal is genuinely a progressive-enhancement layer, not the only way to view a project's details. If a future phase wants React islands, this modal is not currently built to be ported into one — it would be a rewrite, not a wrap.

## Decision: "Hire Me" is a real, backend-processed form (Phase 8) — no email integration, admin-only review

- Status: Accepted
- Date: 2026-08-25 (Phase 8)
- Context: Unlike the Contact page (deliberately static — see the "Timeline cut ... Contact gets no further scheduled work" decision above), Phase 8 needed to decide what "Hire Me" actually does. The owner was asked directly: a dedicated page with a real form, versus a simple CTA linking to Contact.
- Decision: A dedicated `/hire/` page in `core` (per the existing Designs/Blog/Resume/Timeline/Hire-Me → `core` placement decision) with a real `HireRequestForm` (Django `ModelForm` over a new `core.HireRequest` model: name, email, project type, details, optional budget/timeline). Submissions persist to the database (POST/redirect/GET pattern) and are reviewable via Django admin — no outbound email is sent; the project has no email backend/SMTP configuration anywhere, and none was set up for this.
- Reasoning: Owner's explicit choice between the two options offered. Admin-based review was chosen over adding email delivery because it needs no new configuration/credentials and the project already uses Django admin as its content-management surface (e.g. `projects.Project`).
- Consequences: The owner must check `/admin/` to see Hire Me leads — there is no notification (email, etc.) when one arrives. If that becomes a problem, a future phase would need to add an email backend (e.g. Django's SMTP `EmailBackend`) — not scoped here. This is also the project's first real, persisted, user-submitted data (as opposed to Contact's fully client-only, non-submitting form).

## Decision: `Project` model fields and placeholder rows seeded now via data migration (Phase 8)

- Status: Accepted
- Date: 2026-08-25 (Phase 8)
- Context: Phase 8 needed a `Project` model shape and a decision on whether to populate it now (with placeholder-quality data, so the model/views/modal could actually be exercised) or leave the table empty until Phase 11 ("Seeding Real Projects"). The owner was asked directly and chose to seed placeholder rows now.
- Decision: `Project` fields: `title`, `slug`, `short_description`, `tags` (plain comma-separated `CharField`, not a separate tag model), `category`, `role`, `year`, `status` (all freeform `CharField`s, no enum/choices — matches the site's existing freeform placeholder vocabulary), `github_url`/`live_url` (both optional `URLField`s — a project may have neither, either, or both), `order`, `featured`. Three rows mirroring Phase 3's old hardcoded "Project One/Two/Three" placeholder text were seeded via a `RunPython` data migration (`projects/migrations/0002_seed_placeholder_projects.py`, reversible), one flagged `featured=True`, rather than entered by hand through the admin.
- Reasoning: Owner's explicit choice, given directly when asked. A data migration (vs. a manual admin step or a fixture loaded outside the migration chain) makes the seed reproducible by anyone running `manage.py migrate` on a fresh database, consistent with how the rest of the schema is tracked.
- Consequences: The Projects page and Home's featured card are no longer empty, but their content is still explicitly placeholder-quality — not real project data. Phase 11 will need to either edit these 3 rows in place (via admin) or replace them outright; either is fine since the seed migration's job (proving the model/views/modal work) is already done. The one visual simplification this forced: the Projects page's per-entry "browser mockup" is now one shared chip/line pattern generated from each `Project`'s `tags`, rather than Phase 3's three bespoke, hand-crafted mockup layouts (which can't be reproduced generically from arbitrary model data) — not a regression, just a necessary consequence of the content becoming dynamic.

## Decision: Contact form made real/backend-processed too (post-Phase 8 addition) — revisits "Contact gets no further scheduled work"

- Status: Accepted
- Date: 2026-08-25 (post-Phase 8, ad-hoc addition, not a renumbered phase)
- Context: The "Timeline cut from the roadmap; Phase 6 merged into a redefined Phase 8" decision above had explicitly said Contact needed no further scheduled work, considering the static page from Phases 3/5 sufficient, and that "the 'working, backend-processed contact form' idea ... is not carried forward ... unless the owner raises it again later." After Phase 8 shipped Hire Me as a real backend-processed form, the owner did raise it again — asking directly for Contact to get the same treatment: a real table for submissions, not just a static page.
- Decision: Added `core.ContactRequest` (name, email, message, submitted_at) mirroring `HireRequest`'s shape, and a `ContactRequestForm`. `core.views.contact` now follows the same POST/redirect/GET pattern as `hire_me` — the Contact page's form (previously plain, non-submitting HTML with a "this form isn't connected yet" note) now actually saves submissions, shown via a success message on `?sent=1`. Registered in Django admin the same way as `HireRequest`, for the same reason (no email backend exists in the project — admin is the review surface).
- Reasoning: Owner's explicit direction, given directly when asked to add this. Reusing the exact `HireRequest`/`hire_me` pattern (model shape, view shape, template shape) keeps the two "get in touch" surfaces consistent rather than inventing a second approach.
- Consequences: The "Contact gets no further scheduled work" line in the decision above is superseded by this entry for the specific "backend-processed form" point — Contact is otherwise unchanged (same links, same page structure). Like Hire Me, there is no outbound notification when a submission arrives; the owner checks `/admin/`.

## Decision: Resume PDF becomes model-backed/admin-uploadable (post-Phase 8 addition) — resolves the Phase 7 PDF deferral

- Status: Accepted
- Date: 2026-08-25 (post-Phase 8, ad-hoc addition, not a renumbered phase)
- Context: Phase 7's "Blog and Resume ship as static placeholder pages" decision above deliberately wired the Resume page's "Download PDF" button to a static path (`static/files/resume.pdf`) that doesn't exist on disk, noting the owner wanted to hold the real PDF back "until the page turns dynamic." The owner then asked for a real table for the resume PDF too, following the same pattern as Hire Me/Contact.
- Decision: Added `core.Resume` (`file` — a `FileField(upload_to="resume/")` — plus `uploaded_at`, ordered newest-first). `core/views.py`'s `resume` view looks up `Resume.objects.first()`; if one exists, the page's "Download PDF" button links to its real uploaded file (served from `MEDIA_URL`/`MEDIA_ROOT`, newly configured in `config/settings.py`, and served via `static()` in `core/urls.py` when `DEBUG=True`); if none exists yet, it falls back to the original `static/files/resume.pdf` link unchanged (so nothing breaks before the owner uploads one). Registered in Django admin, where the owner uploads/replaces the PDF directly — no separate upload UI on the public site.
- Reasoning: Owner's explicit direction, given directly when asked. A `FileField` + admin upload is the natural Django way to let the owner swap the PDF without touching code or the filesystem by hand, and matches the model-per-feature pattern already established by `HireRequest`/`ContactRequest`/`Project`. `media/` was already listed in `.gitignore` (present since project setup, unused until now) — uploaded resume files are user content, not committed to the repo, same treatment as `static/css/main.css`'s build-artifact exclusion.
- Consequences: This is the project's first `FileField`/`MEDIA_URL` usage — `MEDIA_ROOT` (`media/` at the repo root) now exists as a runtime-created, gitignored directory. The `DEBUG`-gated `static()` media serving in `core/urls.py` is a development convenience only; a production deployment (Phase 13) will need a real media-serving story (e.g. a CDN/object storage or the web server serving `MEDIA_ROOT` directly) — not addressed by this change. If the owner never uploads a `Resume` row, the site's behavior is unchanged from Phase 7 (same missing-file fallback link).

## Decision: Home hero section made model-backed/admin-editable (post-Phase 8 addition)

- Status: Accepted
- Date: 2026-08-25 (post-Phase 8, ad-hoc addition, not a renumbered phase)
- Context: The owner wanted to be able to change the Home hero section's portrait, greeting, hero lines (`I BUILD.` / `I WRITE.` / name / `I EXPLORE.`), roles line, the meta block's three "currently" lines, and the year/location — all previously hardcoded directly in `home.html` since Phase 5's real-content swap — directly from the database/admin, without editing template code, matching the pattern already established by `Resume`.
- Decision: Added `core.HeroContent` (`portrait` — `ImageField(upload_to="hero/")`, `greeting`, `line_one`/`line_two`/`name_first`/`name_last`/`line_three`, `roles`, `meta_line_one`/`two`/`three`, `year`, `location`, `updated_at`), admin-registered. A data migration (`core/migrations/0005_seed_hero_content.py`) seeded one row with Phase 5's real hero copy, and a second migration (`core/migrations/0006_backfill_hero_portrait.py`) copied the existing `static/images/hero-portrait.png` file into that row's `portrait` field (`media/hero/hero-portrait.png`), so the database fully owns the image too, not just the text. `core/views.py`'s `home` view passes `HeroContent.objects.first()` as `hero`; `home.html`'s hero section reads every field as a bare `{{ hero.<field> }}` (portrait as `{{ hero.portrait.url }}`) — **no hardcoded fallback text or fallback image path remains in the template**. `{% load static %}` was removed from `home.html` since nothing in it references `{% static %}` anymore.
- Reasoning: Owner's explicit direction, given directly when asked — an initial version of this decision kept per-field `default:"..."` template fallbacks matching the real copy (so the page wouldn't go blank if the row were ever deleted), but the owner flagged that as still having the real content "written in code" in a second place and asked for it removed entirely, with everything sourced from the database only.
- Consequences: The Home hero section now has a **single source of truth** (the `HeroContent` row) for its copy and portrait — editing it only requires admin, never a template change. The tradeoff: if the `HeroContent` row is ever deleted, the hero section renders blank text and a broken portrait image (Django template variable resolution fails silently — no server error — but there is no content fallback anymore). This is an accepted tradeoff for this specific section, not a general policy — `Resume`'s "Download PDF" button still falls back to a static path if no `Resume` row exists, since that fallback isn't duplicated real content, just a graceful degrade. `core` now holds four models (`HeroContent`, `Resume`, `ContactRequest`, `HireRequest`) — still well within the app's documented "identity, etc." + site-wide-feature role; no boundary concerns with `projects`.
- Follow-up (2026-08-25): the owner asked for Home's "Statement" section — the single centered line, "Good ideas deserve more than thought—they deserve to be built.", between the featured-project card and the about-snapshot section — to move into this same `HeroContent` table too, rather than a new one. Added a `statement` `CharField` to `HeroContent` (default set to that exact real line, so the existing row picked it up automatically via the migration's `AddField` default — no separate data migration needed, unlike the portrait backfill). `home.html`'s statement section is now `{{ hero.statement }}`, no hardcoded fallback, consistent with the rest of `HeroContent`'s fields.
- Follow-up (2026-08-25): after seeing the admin's "Hero contents" list showing a single row, the owner asked for a **second row** dedicated to the quote, rather than a `statement` field shared on the one identity row. Since the site had been reading `HeroContent.objects.first()` for everything, a second undifferentiated row would have been ambiguous (which row supplies the portrait/greeting vs. the quote?) — resolved by adding a `role` field (`choices`: `primary` / `quote`, default `primary`) to `HeroContent` so each row's purpose is explicit and admin-visible (added to `HeroContentAdmin.list_display`/`list_filter`). A data migration (`core/migrations/0009_split_quote_row.py`) created the second row (`role="quote"`) carrying the existing quote text, leaving the original row (`role="primary"`) as the identity row. `core/views.py`'s `home` view now queries `HeroContent.objects.filter(role=HeroContent.ROLE_PRIMARY).first()` as `hero` and `HeroContent.objects.filter(role=HeroContent.ROLE_QUOTE).first()` as `quote`, separately; `home.html`'s statement section reads `{{ quote.statement }}` instead of `{{ hero.statement }}`.
- Reasoning (role split): Owner's explicit direction, given directly after being asked whether they wanted a second row or just a visible column — they confirmed a second row. An explicit `role` field (rather than relying on row creation order/pk) keeps the two rows' purposes unambiguous in admin and in code, and leaves room for more `role` values later (e.g. a rotating set of `quote` rows) without another schema change.
- Consequences: `HeroContent` rows other than the `primary` one still carry all of the model's other fields (portrait, greeting, lines, etc.) at their defaults — unused by the `quote` row, but present in its admin form; this is a byproduct of keeping "the same table" per the owner's original instruction rather than splitting into a separate model. If the `role="quote"` row is ever deleted, `quote` is `None` and the statement section renders blank (same no-fallback convention as the rest of `HeroContent`). If more than one row shares a `role`, `.first()` uses `Meta.ordering` (`-updated_at`) to pick the most recently updated one.

## Decision: Quote split out of `HeroContent` into its own `Quote` model

- Status: Accepted
- Date: 2026-08-25 (post-Phase 8, ad-hoc addition, not a renumbered phase)
- Context: The "Home hero section made model-backed/admin-editable" decision above had resolved an earlier admin-UX complaint (a shared `statement` field on the primary identity row) by adding a `role` field (`primary`/`quote`) to `HeroContent` and splitting the quote into a second row. In practice this meant the quote row carried all 14 identity-only columns (portrait, greeting, hero lines, name, roles, meta lines, year, location) at meaningless defaults, and admins editing that row still saw the full identity form. The owner flagged this as still too complex for what is really just one string, and initially considered making the identity fields optional so the quote row could leave them genuinely blank — then decided that was still complex and asked instead for a dedicated model.
- Decision: Removed `role` (and the `ROLE_PRIMARY`/`ROLE_QUOTE`/`ROLE_CHOICES` constants) and `statement` from `HeroContent` entirely — it is back to a single-purpose identity model with exactly one real row. Added a new `core.Quote` model (`statement`, `updated_at`) holding just the quote line, registered separately in Django admin (`QuoteAdmin`). `core/views.py`'s `home` view now does `HeroContent.objects.first()` and `Quote.objects.first()` as two independent, single-model queries instead of two `role`-filtered queries against the same table. `templates/pages/home.html`'s Statement section needed no changes — `{{ quote.statement }}` resolves the same way against the new model. Migrated via three migrations following the `0009_split_quote_row.py` reversible-`RunPython` convention: `0010_quote.py` (`CreateModel`), `0011_migrate_quote_data.py` (`RunPython` — copies the existing `role="quote"` row's `statement` into a new `Quote` row, deletes that `HeroContent` row; reversible), `0012_remove_herocontent_role_statement.py` (`RemoveField` role, `RemoveField` statement — auto-reversible since both had defaults).
- Reasoning: Owner's explicit direction, given after being offered the "make fields optional" alternative and finding it still too complex. A dedicated model is the more direct fix for "a table with a bunch of unused columns on one row" than field-level optionality would have been.
- Consequences: `HeroContent` is once again a clean, single-row model (no discriminator field, no unused columns) — this reverses part of the "Home hero section made model-backed/admin-editable" decision's `role`-split follow-up above; that decision's `role` sub-follow-ups are now superseded by this one. `core` now holds five models (`HeroContent`, `Quote`, `Resume`, `ContactRequest`, `HireRequest`). If more standalone site-wide text snippets are ever needed, `Quote` is a template for a small dedicated model rather than bolting another field/row onto `HeroContent`.

## Decision: Home's "A little about me" snapshot section made model-backed too (`core.AboutSnapshot`)

- Status: Accepted
- Date: 2026-08-25 (post-Phase 8, ad-hoc addition, not a renumbered phase)
- Context: Following the same treatment already given to the hero section (`HeroContent`) and the statement line (`Quote`), the owner asked for Home's "A LITTLE ABOUT ME" snapshot section (headline "CURIOSITY. / CREATION. / EXPERIMENTATION.", its supporting sentence, the about paragraph, and the "Currently: Building/Learning/Writing/Exploring" lines) to become admin-editable the same way, rather than staying hardcoded in `home.html` from Phase 5.
- Decision: Added `core.AboutSnapshot` (`eyebrow`, `headline_one`/`two`/`three`, `headline_sub`, `paragraph`, `currently_label`, `currently_building`/`currently_learning`/`currently_writing`/`currently_exploring`, `updated_at`), registered in admin (`AboutSnapshotAdmin`, same shape as `HeroContentAdmin`/`QuoteAdmin`). Seeded via a `CreateModel` + `RunPython` seed migration pair (`0013_aboutsnapshot.py`, `0014_seed_about_snapshot.py`), mirroring `0004_herocontent.py`/`0005_seed_hero_content.py`'s pattern, with the exact real content the section already had. `core/views.py`'s `home` view now also passes `AboutSnapshot.objects.first()` as `about_snapshot`; `home.html`'s snapshot section reads every field as `{{ about_snapshot.<field> }}` — no hardcoded fallback text, same no-fallback convention as `HeroContent`/`Quote`.
- Reasoning: Owner's explicit direction — "same as the selected table [`HeroContent`], create another table for this section." Kept the "Currently" rows as four fixed `CharField`s (not a related child model) to match `HeroContent`'s existing `meta_line_one/two/three` pattern rather than introducing a new one-to-many structure for a fixed 4-item list.
- Consequences: `core` now holds six models (`HeroContent`, `Quote`, `AboutSnapshot`, `Resume`, `ContactRequest`, `HireRequest`). Same accepted tradeoff as `HeroContent`/`Quote`: if the single `AboutSnapshot` row is ever deleted, the section renders blank — no template fallback exists. The About page's own content (not this Home snapshot) remains untouched, hardcoded real text from Phase 5 — this change only covers the Home-page snapshot section.

## Decision: Model-level `default=` content strings removed from `HeroContent`/`Quote`/`AboutSnapshot`

- Status: Accepted
- Date: 2026-08-25 (post-Phase 8, ad-hoc addition, not a renumbered phase)
- Context: `HeroContent`, `Quote`, and `AboutSnapshot` all carried their real copy twice — once as the live database rows (the actual source rendered on the site), and again as Python-level `default=...` values on each field in `core/models.py` (originally added so the seed migrations' `.create()` calls, and any fresh row, would come pre-filled). The owner flagged this as real content still "written in code" in a second place, the same complaint that previously drove `HeroContent`'s template-fallback removal (see "Home hero section made model-backed/admin-editable" above), and asked for `core/models.py` to be content-free.
- Decision: Removed every content-carrying `default=` from `HeroContent` (`greeting`, `line_one/two/three`, `name_first/last`, `roles`, `year`, `location`), `Quote` (`statement`), and `AboutSnapshot` (`eyebrow`, `headline_one/two/three`, `headline_sub`, `paragraph`, `currently_label`, `currently_building/learning/writing/exploring`) — fields are now bare `CharField`/`TextField` declarations with no default text. `meta_line_one/two/three` (already `blank=True`, no default) and `portrait` (`blank=True, null=True`) were already content-free and untouched. Generated via `manage.py makemigrations` and applied as `core/migrations/0015_remove_content_defaults.py` — a set of `AlterField` operations only; no data changed, since Django's `default` isn't a database-level constraint, just a Python-side fallback for object instantiation. The already-seeded rows (from `0005`, `0006`, `0009`/`0011`, `0013`/`0014`) were unaffected and keep rendering identically.
- Reasoning: Owner's explicit direction. The one-time seed migrations remain the correct, sole place for this content in code — they populate the database once at deploy time, which is different from a model-level `default` that silently re-supplies real text as an ongoing fallback every time a field is left blank.
- Consequences: These fields have no `blank=True` and no default, so creating a *new* `HeroContent`/`Quote`/`AboutSnapshot` row from now on (via admin's "Add" form or the ORM) requires explicitly supplying every field — there is no more silent real-content fallback if a field is omitted. This is consistent with the "single source of truth, no fallback" convention already established for these three models; it only closes the remaining gap where the fallback lived in the model schema instead of the template.

## Decision: Contact email made model-backed (`core.ContactEmail`), wired site-wide via a context processor

- Status: Accepted
- Date: 2026-08-25 (post-Phase 8, ad-hoc addition, not a renumbered phase)
- Context: The real contact email (`shahbazkhan74659@gmail.com`) was hardcoded in three separate templates — Home's CTA section, the Contact page's contact list, and the Hire Me page's contact list — each a literal `mailto:` link plus display text. The owner asked for a dedicated table for the email, following the same pattern as `HeroContent`/`Quote`/`AboutSnapshot`, and for every hardcoded occurrence to be removed.
- Decision: Added `core.ContactEmail` (`email` — `EmailField`, `updated_at`), registered in admin (`ContactEmailAdmin`). Unlike `HeroContent`/`Quote`/`AboutSnapshot` (each queried individually per-view and passed into that view's own context), the email is needed across three unrelated views (`home`, `contact`, `hire_me`), so it's exposed via a new Django context processor (`core/context_processors.py::contact_email`, registered in `config/settings.py`'s `TEMPLATES[0]['OPTIONS']['context_processors']`) rather than being queried and threaded through three views individually — it injects `contact_email` (the `ContactEmail` instance) into every template's context automatically. `templates/pages/home.html`, `contact.html`, and `hire.html` all now read `{{ contact_email.email }}` for both the `mailto:` href and the displayed address, replacing the hardcoded string in all three places. Seeded via `core/migrations/0016_contactemail.py` (`CreateModel`) + `0017_seed_contact_email.py` (`RunPython` seed, same reversible pattern as the other seed migrations) — following the "no content in `core/models.py`" convention established immediately before this (see "Model-level `default=` content strings removed..." above), the model itself carries no default value; the real address lives only in the seed migration and the database row.
- Reasoning: Owner's explicit direction. A context processor (rather than repeating `ContactEmail.objects.first()` in `home`/`contact`/`hire_me`) was chosen because the email is genuinely site-wide, cross-cutting data, not something owned by a single view — the same reasoning that would apply if the brand mark or nav links ever needed to move from static assets to the database. `prototype/index.html` was left untouched — it never held the real address (its own fictional placeholder, `hello@renzomalik.dev`), consistent with the standing "prototype is a reference only" decision.
- Consequences: `core` now holds seven models (`HeroContent`, `Quote`, `AboutSnapshot`, `ContactEmail`, `Resume`, `ContactRequest`, `HireRequest`). Every template rendered through Django's `render()` shortcut now has `contact_email` available for free — if the email is ever needed in `base.html`'s footer or elsewhere, no view changes are required, just a template reference. Same accepted tradeoff as the other content models: if the single `ContactEmail` row is ever deleted, `contact_email` is `None` and `{{ contact_email.email }}` renders blank — no fallback.

## Decision: Whole About page made model-backed (`core.About`)

- Status: Accepted
- Date: 2026-08-25 (post-Phase 8, ad-hoc addition, not a renumbered phase)
- Context: `templates/pages/about.html` still held its Phase 5 real content entirely hardcoded — the `about` view was a bare `render()` with no context. The owner asked for the whole page to get the same treatment as `HeroContent`/`Quote`/`AboutSnapshot`/`ContactEmail`: a dedicated table, seeded with the current real content, every hardcoded string removed. Asked directly whether structural section headings/labels ("Background", "PHILOSOPHY", "SKILLS & TOOLS", "Currently learning", etc.) should also be database fields or stay fixed in the template, the owner chose full editability — everything on the page becomes a field, matching the precedent already set by `AboutSnapshot.currently_label`.
- Decision: Added `core.About` — one model, one real row, fields mapping 1:1 onto the page's five sections (`eyebrow`, `title_line_one`/`two`, `lede`; `background_heading`/`background_paragraph`; `care_heading`/`care_paragraph`; `philosophy_eyebrow`/`philosophy_quote`; `skills_eyebrow` plus three label+comma-separated-tags pairs — `languages_label`/`languages`, `frameworks_label`/`frameworks`, `learning_label`/`learning`; `going_eyebrow`/`going_paragraph`). The three variable-length skill-tag groups (4/6/2 tags) reuse `projects.Project`'s existing `tags` CharField + `tag_list()` method pattern (`projects/models.py`) rather than introducing a new child/related model — each is one comma-separated `CharField` plus a `languages_list()`/`frameworks_list()`/`learning_list()` splitter method on `About` itself, and `about.html`'s three skill-tag loops now read `{% for tag in about.languages_list %}` etc., the same loop shape `home.html` already used for `featured.tag_list`. No `default=` on any field, per the "Model-level `default=` content strings removed" convention above — real content lives only in the seed migration (`core/migrations/0018_about.py` `CreateModel` + `0019_seed_about.py` `RunPython` seed, mirroring `0016_contactemail.py`/`0017_seed_contact_email.py`'s shape, not the older `0013`/`0014` `AboutSnapshot` pair). `core/views.py`'s `about()` now queries `About.objects.first()` and passes it as `about` — view-local, not a context processor, since no other template references About-page-specific content. Registered in admin (`AboutAdmin`, same shape as `AboutSnapshotAdmin`). The closing "GET IN TOUCH →" link stays hardcoded — a static nav CTA reusing `{% url 'contact' %}`, same treatment as Home's "MORE ABOUT ME" link, not page content.
- Reasoning: Owner's explicit direction, both for the table itself and for making headings/labels fields too when asked directly. Reusing `Project.tag_list()`'s established pattern for the skill groups avoids introducing a new one-to-many model for what the codebase already treats as a flat comma-separated list.
- Consequences: `core` now holds eight models (`HeroContent`, `Quote`, `AboutSnapshot`, `About`, `ContactEmail`, `Resume`, `ContactRequest`, `HireRequest`). Same accepted no-fallback tradeoff as the other content models: if the single `About` row is ever deleted, the page renders blank. Note `templates/pages/resume.html`'s Skills section (Phase 7) independently hardcodes the same skill tags as its own verbatim copy (a deliberate Phase 7 choice, not sourced from a model) — this change did not touch it, since it's a separate page outside this task's scope.

## Decision: GitHub/LinkedIn display names added to `ContactEmail`; Hire Me page gained GitHub/LinkedIn rows

- Status: Accepted
- Date: 2026-08-25 (post-Phase 8, ad-hoc addition, not a renumbered phase)
- Context: The Contact page's GitHub and LinkedIn rows displayed literal hardcoded strings (`github.com/shahbazkhan74659-crypto`, `linkedin.com/in/shahbaz-khan-471aa2418`) as their visible text, while the Hire Me page had no GitHub/LinkedIn rows at all. The owner asked for these "names" to be added to the existing `ContactEmail` table, seeded, and wired onto both the Contact and Hire Me pages, with the hardcoded copies removed. Asked directly whether this should also change the *linking* behavior — Contact's GitHub row deliberately links to the site's own `/github/` page (`{% url 'github' %}`, live-fetched data — see the standing "GitHub gets a custom in-site page" decision), while LinkedIn links straight out to the external URL — the owner clarified only the **display text** ("names") should move to the database; the linking mechanism for both rows stays exactly as it already was, untouched.
- Decision: Added `github_display`/`linkedin_display` (`CharField`) to `core.ContactEmail`, holding just the visible domain-style strings — not full/validated URLs, since they're display text only, not used as href values. `templates/pages/contact.html`'s existing GitHub/LinkedIn rows now render `{{ contact_email.github_display }}`/`{{ contact_email.linkedin_display }}` as their visible text, with both rows' `href` attributes left completely unchanged (`{% url 'github' %}` for GitHub, the literal external LinkedIn URL for LinkedIn). `templates/pages/hire.html` gained two new rows, copied verbatim in structure/markup/href from Contact's, since it had none before. Added via `core/migrations/0020_contactemail_links.py` (`AddField` × 2, `preserve_default=False` — a one-time empty-string backfill for the already-existing `ContactEmail` row, not a permanent model default) + `0021_seed_contact_links.py` (`RunPython`, sets the real display strings on that row; reversible).
- Reasoning: Owner's explicit direction, including the clarification that "linking logic" (which mechanism each row uses) is not the same thing as the "name" (display text) — only the latter counts as content that shouldn't live in code. Kept the model named `ContactEmail` rather than renaming it, per the owner's own phrasing ("in the Contact Email table").
- Consequences: `ContactEmail` now holds four real fields (`email`, `github_display`, `linkedin_display`, plus `updated_at`) — it's grown from a single-purpose email row into the site's general "contact details" row; if more contact-detail rows are ever needed, they likely belong here too rather than a new table, unless a genuinely different concern emerges. The GitHub row's href still intentionally does not use `github_display`'s value as a link target — the two are independent by design (display text vs. routing).

## Decision: Hire Me's "Available for"/"Stack" rows added to `ContactEmail`

- Status: Accepted
- Date: 2026-08-25 (post-Phase 8, ad-hoc addition, not a renumbered phase)
- Context: The Hire Me page's "Available for" ("Freelance / contract") and "Stack" ("Django · React · Tailwind") rows were still fully hardcoded — both the labels and the values. The owner asked for these to be added to the same `ContactEmail` table, seeded, and wired in the same way as the earlier GitHub/LinkedIn display-name addition, with the hardcoded copies removed.
- Decision: Added `available_for_label`/`available_for` and `stack_label`/`stack` (all `CharField`) to `core.ContactEmail`. Unlike the GitHub/LinkedIn addition (where only the display text moved to the database and the label words "GitHub"/"LinkedIn" stayed as fixed markup), both the label and the value are database fields here — these rows have no separate "linking logic" concern (plain text, no `href`), so the full-editability convention already established for `AboutSnapshot`/`About` applies directly with no exception needed. `templates/pages/hire.html`'s two rows now render `{{ contact_email.available_for_label }}`/`{{ contact_email.available_for }}` and `{{ contact_email.stack_label }}`/`{{ contact_email.stack }}`. Added via `core/migrations/0022_contactemail_hire_fields.py` (`AddField` × 4, `preserve_default=False` — one-time empty backfill for the existing row) + `0023_seed_contact_hire_fields.py` (`RunPython` seed with the real values; reversible).
- Reasoning: Owner's explicit direction, reusing the exact same table/migration pattern as the prior GitHub/LinkedIn addition.
- Consequences: `ContactEmail` now holds eight real fields beyond `id`/`updated_at` (`email`, `github_display`, `linkedin_display`, `available_for_label`, `available_for`, `stack_label`, `stack`) — it has fully become the site's general "contact page details" row rather than a single-purpose email table, continuing the trajectory noted in the GitHub/LinkedIn decision above.

## Decision: New Phase 11 "Custom Admin Hub" added; former Phase 11/12/13 renumbered to 12/13/14

- Status: Accepted
- Date: 2026-08-25
- Context: The build roadmap had already been extended once (see "New Phase 11 'Seeding Real Projects' added" above) to Phase 0 through Phase 13. By this point, `core` alone holds eight content/request models (`HeroContent`, `Quote`, `AboutSnapshot`, `About`, `ContactEmail`, `Resume`, `ContactRequest`, `HireRequest`) plus `projects.Project`, all managed exclusively through Django's default `/admin/` — the owner asked for a new phase covering a custom, central, user-friendly CRUD hub for the whole site, to be inserted at Phase 11 (pushing every phase from the current Phase 11 onward one step later), rather than appended at the end of the roadmap.
- Decision: Insert a new Phase 11, "Custom Admin Hub," positioned after Phase 10 (production database chosen) and before the seeding/testing/deployment phases — same insertion point precedent as the earlier "Seeding Real Projects" addition. The former Phase 11 "Seeding Real Projects" becomes Phase 12; the former Phase 12 "Full End-to-End Testing" becomes Phase 13; the former Phase 13 "Deployment and Hosting" becomes Phase 14. While renumbering, a stale internal cross-reference in the (now) Phase 14 scope text — which had said "Phase 11 (testing)" even though testing was already Phase 12 at the time, a leftover from the previous renumbering that missed this one cross-reference — was corrected to point at the (now) correct Phase 13.
- Reasoning: Owner's explicit direction — inserted at Phase 11 specifically, ahead of data-seeding/testing/deployment, since a central admin hub is infrastructure for managing content (including the real project data Phase 12 will seed) rather than a finishing-touch task suited to the end of the roadmap.
- Consequences: `PHASES.md`'s intro note and closing "last phase" line were updated to reflect Phase 0–14 instead of Phase 0–13, and now record two separate phase-insertion events. Live cross-references to the old "Phase 11 = Seeding Real Projects" numbering in `TASKS.md` and `ARCHITECTURE.md` were updated to Phase 12. Per this file's own historical-record convention (see `CLAUDE.md` rule 7), earlier `DECISIONS.md` entries that mention "Phase 11" in prose (e.g. the `Project` model fields decision, referring to "Phase 11 ('Seeding Real Projects')") are left unedited — they correctly reflect the numbering as of their own date and are not live pointers. This is a second live example of `CLAUDE.md`'s "if new phases are identified later, add them here rather than assuming the roadmap is fixed forever" guidance.

## Decision: New Phase - X "Blog Upgrade: Content, Logic, and Tables" added after Phase 10, without renumbering Phases 11–14

- Status: Accepted
- Date: 2026-08-25
- Context: `/blog/` has shipped since Phase 7 as a static placeholder page (three hardcoded post entries, no model — see "Blog and Resume ship as static placeholder pages" above), with real blog content and a `Post`-style model explicitly left unscheduled to any phase. The owner asked for a new phase covering the Blog page's full upgrade — real content, real logic, and real database tables — to be inserted right after Phase 10, this time explicitly instructing that Phases 11–14 keep their current numbers rather than shifting again.
- Decision: Insert a new phase between Phase 10 and Phase 11, labeled **Phase - X** (a non-numeric placeholder label, not "Phase 11") specifically so the existing Phase 11–14 numbering is left untouched, per the owner's explicit instruction. Phase - X's objective: replace Phase 7's placeholder Blog page with a real `Post`-backed feature — a model/table for posts, the list/detail view and URL logic to serve them, and the owner's real blog writing replacing the placeholder entries. Detailed scope/completion criteria left "to be defined," same as other under-specified phases (e.g. Phase 9).
- Reasoning: Owner's explicit direction, including the specific instruction not to renumber further phases this time — unlike the two prior phase insertions (both handled by shifting every subsequent phase number up by one), this one uses a non-numeric label to preserve the existing Phase 11–14 numbers.
- Consequences: The roadmap's numbering is no longer a strict contiguous integer sequence — Phase - X sits in build order after Phase 10 and before Phase 11 without a number of its own. `PHASES.md`'s intro note and closing line were updated to describe this as a third, differently-handled insertion event. `ARCHITECTURE.md`'s "Planned" note on real blog posts was updated to point at Phase - X instead of "no phase scheduled yet." If another unnumbered phase is ever inserted, it will need its own distinguishing label (not also "X") to stay unambiguous — this convention has no precedent yet for a second such insertion.

## Decision: New Phase Y "Project Page Upgrade: Logic and Content-Ready Interface" added after Phase - X, without renumbering Phases 11–14

- Status: Accepted
- Date: 2026-08-25
- Context: Following the "New Phase - X ... added after Phase 10" decision immediately above, the owner asked for a further phase covering a logic and interface upgrade to the Projects page, to be inserted right after Phase - X — again without renumbering Phases 11–14.
- Decision: Insert a new phase directly after Phase - X and before Phase 11, labeled **Phase Y** — the same non-numeric-label convention as Phase - X, chosen for the same reason (preserve the existing Phase 11–14 numbers). Phase Y's objective: further logic and interface work on the `projects` app (building on Phase 8's `Project` model/list/detail/modal mechanism) so the Projects page's interface is fully content-ready — distinct from Phase 12 "Seeding Real Projects," which populates the data itself rather than the page's logic/interface. Detailed scope/completion criteria left "to be defined," same as Phase - X and other under-specified phases.
- Reasoning: Owner's explicit direction, extending the labeling convention established one decision earlier for Phase - X to a second unnumbered insertion, keeping build order explicit (Phase 10 → Phase - X → Phase Y → Phase 11) without touching the numeric sequence.
- Consequences: The roadmap now has two consecutive non-numeric phases (X, then Y) between Phase 10 and Phase 11. `PHASES.md`'s intro note and closing line were updated to record this as a fourth phase-insertion event and to make the X-then-Y ordering explicit. If a further unnumbered phase is inserted later, per the note in the Phase - X decision above, it will need its own distinguishing label — "X" and "Y" are now both taken.

## Decision: Phase 9 scope confirmed via a codebase-grounded survey; SEO/meta explicitly excluded

- Status: Accepted
- Date: 2026-08-25 (Phase 9)
- Context: Phase 9's scope had read "To be defined by the project owner" since it was first written. Rather than propose scope from generic best-practice knowledge, the codebase was surveyed for concrete polish candidates across 9 categories (error pages, forms, accessibility, responsive design, SEO/meta, performance, code quality, security basics, testing), each finding tied to an actual file/line. The owner was then asked to select which candidates belong in Phase 9, presented as two grouped multi-select questions (frontend/backend).
- Decision: Phase 9's scope is exactly 8 items the owner selected: accessibility fixes, responsive/mobile gaps, visible form validation errors, smooth scrolling behaviour (frontend); custom error pages, production-safety settings, GitHub API caching, view code de-duplication (backend). SEO/meta (favicon, Open Graph tags, robots.txt) was surveyed but **not** selected — explicitly out of scope, not an oversight. Testing-related findings were not offered as Phase 9 candidates at all, since Phase 12 already owns that scope.
- Reasoning: Grounding the candidate list in the actual code (rather than a generic "polish checklist") meant the owner was choosing between real, concrete trade-offs specific to this project, not abstract categories — consistent with this project's established pattern of the owner making explicit choices rather than Claude assuming scope on their behalf.
- Consequences: `PHASES.md`'s Phase 9 entry now has real Scope/Completion Criteria text instead of "to be defined." If SEO/meta work is wanted later, it has no phase yet — same situation as Designs.

## Decision: Phase 9 implementation choices — `python-dotenv`, default `LocMemCache`, error pages extending `base.html`

- Status: Accepted
- Date: 2026-08-25 (Phase 9)
- Context: Three of Phase 9's backend items needed a specific technical approach, not just a scope decision: (1) externalizing `SECRET_KEY`/`DEBUG`/`ALLOWED_HOSTS` needed an env-var loading mechanism — none existed in the project; (2) caching the GitHub API calls needed a cache backend — none was configured; (3) the three new error templates needed to decide whether to extend `base.html` (the site's only shared shell) or stand alone.
- Decision:
  1. **`python-dotenv`** over `django-environ` or raw `os.environ.get()`: only 3 settings needed externalizing, so `django-environ`'s casting/`DATABASE_URL` machinery was unnecessary weight, and plain `os.environ.get()` alone doesn't load a `.env` file — something still has to populate `os.environ` from it. `config/settings.py` calls `load_dotenv(BASE_DIR / '.env')` once at the top, then reads each setting with a dev-safe default identical to the old hardcoded value, so behavior is unchanged for anyone without a `.env` file. A tracked `.env.example` documents the three keys.
  2. **Django's default `LocMemCache`**, not a new dependency: `config/settings.py` has no `CACHES` setting, and Django 5.2 uses `LocMemCache` automatically in that case — `django.core.cache.cache` worked with zero new configuration. Two cache keys (profile, repos), 15-minute TTL, only successful responses cached.
  3. **All three error templates (`404.html`/`500.html`/`403.html`) extend `base.html`**, verified against Django 5.2's actual `django/views/defaults.py`: the `404`/`403` views pass `request` into the template (so `base.html`'s nav/footer, `{% url %}`, and context processors all work normally), but the `500` view (`server_error`) does **not** pass `request` at all. This is safe for `base.html` specifically because its only `request`-dependent expression is the nav's `aria-current` active-link check, which Django resolves as falsy rather than erroring when `request` is undefined — so the 500 page still renders a working nav/footer, just without the (irrelevant, on an error page) active-link highlight.
- Reasoning: Each choice picked the option needing the least new surface area (no new dependency where Django already provides one; the lightest env-var library for the actual number of settings involved) while still verifying correctness against real behavior (Django source, not assumption) for the one genuinely nuanced piece — the 500 page's missing request context.
- Consequences: `requirements.txt` gained exactly one new dependency (`python-dotenv==1.1.0`). `LocMemCache`'s per-process nature (not shared across multiple prod workers) is noted in `ARCHITECTURE.md`'s Important Invariants as a future consideration, not an issue now. If a future page's error template needs `request`-dependent content beyond what `base.html` currently uses, the 500 case specifically would need re-checking — it cannot assume `request` is available.

## Decision: Contact/Hire Me views merged into a shared `_handle_lead_form` helper

- Status: Accepted
- Date: 2026-08-25 (Phase 9)
- Context: `core.views.contact` and `core.views.hire_me` had near-identical POST/redirect/GET logic since Phase 8 (validate `ModelForm`, save on valid POST, redirect to `f"{request.path}?sent=1"`, render with `sent` context on GET) — flagged during the Phase 9 codebase survey as duplicated code, and selected by the owner as an in-scope item ("View code de-duplication").
- Decision: Added `core._handle_lead_form(request, form_class, template_name)` implementing the shared logic once (plus the new Phase 9 error-attribute tagging — `aria-invalid`/`aria-describedby` on invalid fields, for the "visible form validation errors" item). `contact`/`hire_me` are now one-line wrappers calling it with their respective form class and template. Behavior preserved exactly: same redirect target, same GET `?sent=1` detection, same template context shape.
- Reasoning: Owner's explicit selection during Phase 9 scoping; a straightforward extract-shared-helper refactor with no behavior change, verified via direct POST requests (invalid submission showed errors, valid submission still redirected to `?sent=1` and saved).
- Consequences: Any future lead-capture form with this same shape should reuse or extend `_handle_lead_form` rather than duplicating the pattern again — recorded as a precedent in `ARCHITECTURE.md`'s Important Invariants.

## Decision: Prototype is a styling/design reference only — production build starts from scratch

- Status: Accepted
- Date: 2026-08-24
- Context: The relationship between `prototype/index.html` and the planned Django + Tailwind + React production build was previously unconfirmed (see decision above).
- Decision: The prototype is not to be ported, reused, or built upon as code. Production implementation will be built from scratch on the confirmed stack (Django + Django templates + Tailwind CSS + React/JS + relational database). The prototype's HTML/CSS is retained only as a visual/styling and layout-structure reference to consult while building the real templates and components.
- Reasoning: Owner's explicit direction.
- Consequences: Do not treat `prototype/index.html` as a source to copy code from during production scaffolding (Phase 3) — reimplement the design intent in Django templates/Tailwind/React rather than lifting its vanilla-JS routing/rendering approach. Its placeholder persona ("Renzo Malik") and placeholder project content are not real content and must not carry over either. See `PHASES.md` and `TASKS.md`.

## Decision: Phase 10 stack — Neon (PostgreSQL) + Render (hosting) + UptimeRobot (keep-alive), interim until an international card enables Oracle Cloud

- Status: Accepted
- Date: 2026-08-26
- Context: Phase 10 needed to resolve two deferred questions at once: the production database engine (PostgreSQL vs. MySQL, left open since the original stack decision — see "Production stack" decision above) and, since none had been chosen yet, an actual hosting platform to run the Django app on. The owner currently has no credit/debit card, which rules out every VPS/IaaS option (Oracle Cloud, GCP, AWS, Azure) — all of them require a card for identity verification even on their genuinely-free-forever tiers (Oracle's Always Free, GCP's Always Free e2-micro, etc.). A survey of card-free PaaS options found Koyeb closed its free tier to new signups after its February 2026 acquisition by Mistral AI, and PythonAnywhere's free tier restricts outbound network connections to a whitelist that would block a raw Postgres connection to an external host like Neon (its free-tier database offering is MySQL only, hosted on PythonAnywhere itself). Render (no card required in the general case) surfaced as viable but has a 15-minute-idle spin-down with a 30–60s cold start on the next request, which the owner flagged as unacceptably slow for a first-time visitor.
- Decision: Deploy on **Render** (Django app hosting, free tier, no card) + **Neon** (managed PostgreSQL, free tier, no card, 0.5 GB storage/100 compute-hours per month) + **UptimeRobot** (free external ping monitor, ~5-minute interval) hitting the live site to keep Render's instance from spinning down, eliminating the cold-start delay for real visitors. This resolves Phase 10's database engine choice as **PostgreSQL** (via Neon), not MySQL. This is an explicitly interim arrangement — once the owner obtains an international debit or credit card, the plan is to migrate to **Oracle Cloud's Always Free tier** (a real always-on VM, no PaaS cold-start concern at all).
- Reasoning: Owner's explicit choice after weighing the alternatives directly: Zeabur (no card, native few-second cold start) was also viable but is a newer, smaller platform with some reported free-tier reliability quirks; Render is the more mainstream, better-documented Django host, and the UptimeRobot keep-alive ping is a widely-used, zero-cost way to neutralize its one real downside. The Oracle Cloud migration is deferred purely on the card blocker, not on any technical shortcoming of Oracle's offering.
- Consequences: `requirements.txt`/`config/settings.py` will need a Postgres driver (e.g. `psycopg`) and a `DATABASE_URL`-style config pointing at Neon in production, while keeping SQLite for local dev (per `ARCHITECTURE.md`'s existing dev/prod split). The UptimeRobot keep-alive is an unofficial workaround, not a Render-supported feature — if Render's idle-detection behavior ever changes, this could stop working and would need revisiting. Migrating to Oracle Cloud later means re-provisioning the app on a real VM (not a PaaS redeploy) and moving the Postgres data off Neon — not yet scoped, tracked as a future follow-up once a card is available, not a numbered phase change. See `PHASES.md`'s Phase 10 entry and `TASKS.md`. **Partially superseded 2026-08-26 — see the two decisions immediately below:** the actual Render deployment + UptimeRobot setup moved to Phase 14, and local development moved from SQLite to a local PostgreSQL install rather than staying on SQLite or Neon day-to-day.

## Decision: `config/settings.py` wired to PostgreSQL via `DATABASE_URL`, connected to a new Neon project

- Status: Accepted
- Date: 2026-08-26 (Phase 10)
- Context: Following the Phase 10 stack decision above, the database engine choice (PostgreSQL via Neon) needed to actually be implemented in code, not just decided.
- Decision: Added `psycopg`/`psycopg-binary` 3.3.4 to `requirements.txt`. `config/settings.py`'s `DATABASES` now branches on a `DATABASE_URL` environment variable: if set, it's parsed with `urllib.parse` (stdlib only — no `dj-database-url`/`django-environ` added, consistent with the project's established minimal-dependency convention, see the Phase 9 `python-dotenv` decision above) into a `django.db.backends.postgresql` config, carrying any query-string parameters (e.g. `sslmode`, `channel_binding`) through to `OPTIONS` and defaulting `sslmode` to `require` if the URL doesn't specify one; if `DATABASE_URL` is unset, `DATABASES` falls back to the original SQLite config unchanged. A second, separate Neon project (the owner's account already had one other, unrelated project) was created for this portfolio specifically. `manage.py migrate` was run against it (all existing migrations applied cleanly, including the seed-data migrations), and a superuser (`Shahbaz`) was created directly on it via `createsuperuser --noinput`. The connection string lives only in the local, gitignored `.env`; `.env.example` documents the `DATABASE_URL` key with no real value.
- Reasoning: Parsing `DATABASE_URL` by hand with stdlib `urllib.parse` avoids a new dependency for something achievable in ~10 lines, matching the same tradeoff already made for env-var loading in Phase 9. Creating a dedicated second Neon project (rather than reusing the owner's existing one) keeps this portfolio's data fully isolated from whatever else that account hosts.
- Consequences: Any future environment (Render in Phase 14, or a fresh local checkout) just needs a `DATABASE_URL` in its `.env`/environment to use Postgres — no code changes required. SQLite remains the zero-setup fallback for a fresh checkout with no `.env` at all. See the next decision for what happened to *local* development specifically.

## Decision: Local development moved from SQLite to a local PostgreSQL install; Render deployment rescheduled to Phase 14

- Status: Accepted
- Date: 2026-08-26 (post-Phase 10 implementation, ad-hoc addition)
- Context: Immediately after wiring up and verifying the Neon connection, the owner reconsidered two things: (1) rather than deploying to Render right away as part of Phase 10, the owner wants the actual Render deployment to happen at **Phase 14** ("Deployment and Hosting"), which already existed in the roadmap for exactly this purpose — Phase 10 was originally scoped (see the "Phase 10 stack" decision above) as covering both the DB engine *and* the hosting deployment together, but the owner is now splitting those apart. (2) The owner wants local development itself to run on PostgreSQL rather than SQLite (or the remote Neon database) day-to-day. A local PostgreSQL 18 server was found already installed and running as a Windows service on this machine (port 5432, password auth).
- Decision: A dedicated local role (`portfolio_user`) and database (`portfolio`, owned by that role) were created on the local PostgreSQL 18 server — not using the `postgres` superuser role directly for the app's day-to-day connection. The local `.env`'s `DATABASE_URL` now points at this local database (`postgresql://portfolio_user:***@127.0.0.1:5432/portfolio?sslmode=disable` — `sslmode=disable` because the local install has SSL off, PostgreSQL's default when not explicitly configured). `manage.py migrate` was run against it (clean) and a matching local superuser (`Shahbaz`) was created. The Neon connection string used for the prior verification is kept, commented out, in the same `.env` file for reference — Neon is not the active local database anymore, but remains the intended target to reconnect to once Phase 14's Render deployment happens. Phase 10's scope is narrowed back to "choose and connect the production database engine" (now done — PostgreSQL, proven against both a local instance and Neon); the actual Render + UptimeRobot deployment work described in the "Phase 10 stack" decision above moves to **Phase 14**, which will need its scope filled in accordingly (currently still "to be defined" pending the owner's direction on that phase specifically).
- Reasoning: Owner's explicit direction — no reasoning beyond the direct instruction was captured for either sub-decision (moving deployment to Phase 14, or preferring local Postgres over SQLite/Neon for day-to-day dev).
- Consequences: `manage.py runserver` now serves from local PostgreSQL by default on this machine (whenever `.env`'s active `DATABASE_URL` line points at it) — SQLite is still the code-level fallback if `DATABASE_URL` is entirely unset (e.g. a fresh checkout with no `.env` at all), but is no longer what this machine's day-to-day development actually uses. Any other machine/checkout that wants the same local-Postgres setup will need its own local PostgreSQL install, role, and database created the same way — this is a per-machine `.env` concern, not something `settings.py` needs to handle differently. Phase 10's Completion Criteria and Status in `PHASES.md` are updated to reflect the narrower scope; Phase 14's Scope will need the Render/Neon/UptimeRobot detail added when that phase actually starts.

## Decision: Phase Y scope and implementation — `ProjectImage` gallery model, filter/search/pagination, tag-count parity, Home fallback removal

- Status: Accepted
- Date: 2026-08-27 (Phase Y)
- Context: Phase Y's scope had read "To be defined by the project owner" since it was first written. Rather than propose scope from generic best-practice knowledge, the owner was asked directly (via AskUserQuestion) across three questions: (1) whether to add real project screenshots to replace the always-shown decorative `.mockup` chip; (2) what list-page logic to add, given the page had zero filtering/search/sort/pagination; (3) whether to also fix the `Project.tags` admin-validation gap (unlike `Post.tags`, which already enforced 1–6) and remove Home's hardcoded featured-project placeholder fallback.
- Decision: The owner selected: real screenshots (recommended option) plus, via free-text follow-up, a richer detail page ("some screenshots and explanation of the Project with GitHub repository link and Live URL, if not deployed yet, only GitHub repository"); category/tag filtering, free-text search, and pagination (all selected, multi-select); both cleanup items (tag-count parity, Home fallback removal). Implementation choices made to satisfy this scope, each following an existing project convention rather than inventing a new one:
  - **One `ProjectImage` model, not a single `image` field plus a separate gallery model** — `project.primary_image` (a `.images.first()` property) serves the list-card/modal/Home's single image, `project.images.all` serves the detail page's full gallery, avoiding two overlapping concepts. "No image" is expressed by a project having zero `ProjectImage` rows (triggering the `.mockup` fallback), not by a row with a blank `image` field.
  - **`Project.description` added as a separate long-form field**, not overloading the existing 200-char `short_description` (which stays as the list-card/Home summary text) — mirrors how `Post.content` and `Post.short_description` already coexist.
  - **GitHub/Live-URL "if not deployed yet, only GitHub" logic required no code change** — `project_detail.html` and the modal `<template>` already used independent `{% if project.github_url %}` / `{% if project.live_url %}` guards from Phase 8, which already produces exactly this behavior.
  - **Filter/search/pagination is GET-param driven** (`?category=&tag=&q=&page=`), AND-combinable, bookmarkable — following the site's existing query-param convention (`?sent=1` on Contact/Hire Me) rather than introducing client-side JS filtering. This is the project's first list-page filter/search/pagination logic anywhere (Blog's `blog_list` remains unaffected, still a bare `.all()`).
  - **`ProjectAdminForm.clean_tags()`** is a byte-for-byte mirror of the existing `PostAdminForm.clean_tags()`.
  - **Home's `{% else %}` placeholder branch was deleted outright**, not replaced with new fallback copy — matches the "single source of truth, no template fallback" convention already established by `HeroContent`/`Quote`/`AboutSnapshot` (see decisions above).
  - **The project-detail modal stays lightweight** — it gained the primary-image-or-mockup swap (for visual consistency with the card that triggered it) but not the full gallery or the long `description` field, which remain exclusive to `project_detail.html`'s "FULL PAGE →" destination. No changes were needed to the modal's vanilla-JS mechanism itself, since it clones whatever HTML sits inside the matching `<template>`.
- Reasoning: Grounding the scope questions in the actual codebase (not a generic "add filtering" ask) let the owner choose between concrete, specific tradeoffs — consistent with this project's established pattern (see the Phase 9 scope-survey decision below) of the owner making explicit choices rather than Claude assuming scope. Each implementation choice reused an existing in-repo pattern (fallback convention, query-param convention, admin-form-validation convention, media-upload convention) rather than introducing a new one where one already existed.
- Consequences: `projects` migration `0010_project_description_projectimage.py` is purely additive (`AddField` + `CreateModel`, no data migration) — the 3 existing seeded placeholder `Project` rows are valid with zero `ProjectImage` rows and empty `description`, rendering the pre-existing `.mockup` fallback until Phase 12 uploads real content. `ProjectImageInline` is the project's first Django admin inline. `PHASES.md`'s Phase Y entry now has real Scope/Completion Criteria instead of "to be defined." If Blog's list page ever wants the same filter/search/pagination treatment, `project_list`'s shape is the pattern to follow (see `ARCHITECTURE.md`'s Important Invariants) — not scoped to any phase currently.

## Decision: First real Project seeded ad hoc ("TS Library"); all 3 Phase 3/5 placeholders deleted outright

- Status: Accepted
- Date: 2026-08-27 (ad hoc, ahead of Phase 12 "Seeding Real Projects" formally starting)
- Context: Real project content is formally Phase 12's job, but — mirroring how the first real blog post was written ahead of Phase - X's full completion — the owner wanted to add their first real project now, supplying full details via a local file (`c:\Portfolio\project.txt`, read but not acted on until explicitly confirmed the following turn, per the standing "don't fabricate real content" rule). Unlike the blog case (exactly 1 placeholder `Post` swapped for exactly 1 real post), the Projects table held 3 placeholder rows (Project One/Two/Three) with only one (`featured=True`), so what to do with the other two wasn't a clean 1:1 mapping — the owner was asked directly.
- Decision: The owner chose to delete all 3 placeholders outright (not keep any alongside the real project, not just swap the featured one). `projects/migrations/0011_seed_real_first_project.py` deletes all 3 by slug and seeds "TS Library" — a Spring Boot/Thymeleaf/MySQL library management system deployed live on Render (MySQL hosted separately on Aiven) — as the sole `Project`, `featured=True`, `order=1`. Its `status` field value from `project.txt` ("Shipped — it's live and deployed at ts-library.onrender.com.") was trimmed to just `"Shipped"` to fit the model's 30-char `status` field, since the deployment URL is redundant with the separate `live_url` field; this was flagged to the owner in advance with no objection raised. The long-form `description` field's content is `project.txt`'s text verbatim, with only the file's own ASCII formatting artifacts removed (the outer `{ }` wrapper, `==` section dividers) — no wording or facts were altered or invented. 5 new `Tag` rows were created (Java, Spring Boot, Thymeleaf, MySQL, Docker) — none overlap the site's existing tag vocabulary (Django, Python, Tailwind CSS, Relational DB), which is expected since this project uses an entirely different stack than the portfolio site itself. No screenshots were seeded — `project.txt` supplied none — so TS Library renders via the `.mockup` fallback Phase Y built until real `ProjectImage` rows are uploaded via admin.
- Reasoning: Owner's explicit choice on the placeholder-deletion question, given directly when asked (three options were offered: delete all 3, delete only the featured one, or keep all 3 and add TS Library as a 4th row) — "delete all 3" was the recommended and selected option, keeping the site in a clean state (no lingering placeholder rows mixed with real content) rather than a partial state that would need revisiting once more real projects are added.
- Consequences: The Projects page currently shows exactly one project. This is expected and matches Phase 12's still-open scope — more real projects and screenshots for this one are future work, not gated on any specific phase boundary (same as how additional real blog posts aren't gated on a phase). If a future project's `status` text is naturally longer than 30 characters, the same trim-and-drop-the-redundant-detail approach applies, since the field's limit is a modeling decision from Phase 8, not something this addition changed.

## Decision: Phase 11 Custom Admin Hub — new `adminhub` app, custom staff-only login, exact-clone UI with inline CRUD affordances

- Status: Accepted
- Date: 2026-08-28 (Phase 11)
- Context: Phase 11's scope had read "To be defined by the project owner" since it was first written. The owner asked for a central, user-friendly CRUD surface replacing reliance on Django's default `/admin/`, with several explicit constraints given directly: the hub must visually clone the public pages exactly (name/positioning/content/cards); every piece of seeded content must be editable/updatable/deletable; the public Contact/Hire Me forms must NOT be copied into the hub (a small placeholder card in their place instead); no GitHub-page equivalent; exactly 6 pages initially (Home, Projects, About, Contact, Hire Me, Blog) — later expanded to 7 by also including Resume (see the next decision). Follow-up questions (via `AskUserQuestion`) settled: a new dedicated `adminhub` app at `/adminhub/` with its own `templates/adminhub/` directory (owner's explicit instruction, overriding an initially-offered "put it in `core`" option); a custom site-styled login page rather than Django's default `/admin/login/`; Edit affordances open a dedicated full page (not a JS modal); Contact/Hire Me additionally show a "Recent Submissions" list with delete (approved as a scope addition beyond the literal page clone); singleton "settings" models get Edit + Delete only, no Add; tags are managed via an in-form checkbox picker plus an inline "quick add" box.
- Decision: `adminhub` is a fully additive third Django app — `INSTALLED_APPS` and `core/urls.py` each gained exactly one line (`'adminhub'`, `path('adminhub/', include('adminhub.urls'))`); no existing app's models/views/urls/templates were modified except where Resume's own scope required it (see next decision). Auth: `adminhub/decorators.py::hub_staff_required` wraps `django.contrib.auth.decorators.user_passes_test(lambda u: u.is_active and u.is_staff, login_url='adminhub:login')` — deliberately NOT `django.contrib.admin`'s `staff_member_required` (that's tied to `AdminSite.has_permission`/`/admin/login/` conventions), so `/admin/` stays completely independent and unaffected. `adminhub/views/auth.py::hub_login` reuses Django's built-in `AuthenticationForm` (no custom form needed) with an added `is_staff` check before calling `login()`. Each of the 7 hub pages (`adminhub/views/{home,projects,about,contact,hire,blog,resume}.py`) hand-duplicates the corresponding public template's section markup/CSS classes into a new `templates/adminhub/*.html` file, extending a standalone `templates/adminhub/base.html` — a hand-duplicated copy of the real `base.html`'s nav/footer markup, not an `{% extends "base.html" %}` of it (see the follow-up note below for why) — the public templates have no extracted partials today, so duplication (not retrofit-and-share) was the only approach consistent with "don't touch existing templates." Small `.hub-edit-btn`/`.hub-delete-btn`/`.hub-add-btn` affordances were added directly into these new hub templates only; a new CSS section was appended to `static_src/css/input.css` (`.hub-*` classes, reusing existing `--bg`/`--accent`/`--border`/`--font-mono` tokens plus one new `--color-danger` token) — no changes to any existing selector. Singleton models (`HeroContent`, `Quote`, `AboutSnapshot`, `About`, `ContactEmail`, and Resume's new `ResumePage`/`core.Resume` file) share one `singleton_edit_view`/`singleton_delete_view` helper pair (`adminhub/views/_shared.py`); multi-row models (`Project`+`ProjectImage` via `inlineformset_factory`, `Post`, `ResumeExperience`, `ResumeEducation`) get full Add/Edit/Delete. Tags: `partials/tag_picker.html` renders a `CheckboxSelectMultiple` chip list plus a same-page "new tag name" input; the owning view checks for a `quick_add_field` POST key before normal validation, `get_or_create`s the `Tag`, and re-renders the (unsaved) form with the new tag pre-checked and every other typed field intact — no redirect, no AJAX, no new JS at all. The existing `Post`/`Project` 1–6 tag-count validator was duplicated (not cross-imported, to keep hub forms independent of `projects.admin`) into `adminhub/forms.py`, and additionally applied to `About`'s and the new `ResumePage`'s tag M2M fields for consistency (confirmed safe against then-current seed counts).
- Reasoning: Each implementation choice picked the option most consistent with an existing project convention (PRG-style dedicated-page forms matching `core._handle_lead_form`'s precedent; server-rendered-only tag quick-add matching the site's "vanilla JS only where genuinely needed" convention — this needed none at all; a separate auth decorator rather than co-opting Django admin's, to keep `/admin/` provably untouched) rather than inventing new patterns.
- Consequences: `core` and `projects` app code/urls/admin.py are completely unchanged by this phase (only `core/models.py`/`views.py` and `templates/pages/resume.html` changed, and only because of the separate Resume-upgrade decision below). Any future 8th hub page would follow the same hand-duplicated-template + dedicated-view-module pattern. Two visual bugs were found and fixed during live-browser verification, not the test-client pass: (1) `.hub-edit-btn`/`.hub-delete-btn`'s independent fixed `right` offsets collided on cards whose own top-right corner already held content (`.resume-entry`'s `.resume-period` text) — fixed with a scoped `.resume-entry.hub-card{ padding-right: 250px }` rule rather than changing the shared button positioning globally; (2) swapping a `ModelMultipleChoiceField`'s widget to `CheckboxSelectMultiple` post-construction (in `TagPickerFormMixin.__init__`) silently drops the field's queryset-derived choices unless `field.widget.choices` is explicitly re-assigned from `field.choices` afterward — every tag picker was rendering as an empty box until this was fixed. Both are noted here since they're non-obvious Django/CSS gotchas a future editor of these forms/templates could easily reintroduce.
- **Follow-up correction (2026-08-28, same day):** The first implementation had `adminhub/base_hub.html` extend the real public `base.html` and inject a separate "ADMIN HUB" bar (hub-only nav links + logout) underneath it — so every hub page showed *two* stacked headers: the real public nav (linking to public URLs) and the custom admin bar (linking to hub URLs). The owner flagged this directly after seeing it live and asked for it removed. Replaced with a single standalone `templates/adminhub/base.html` (not extending `base.html` at all) that reproduces the public nav/footer markup exactly — same "SHAHBAZ KHAN" brand mark, same four nav links (Projects/About/Contact/Hire Me) and SK badge, same footer link row minus `GitHub` (no GitHub page exists in the hub) — but every link points at the `adminhub:` namespace instead of the public one. The `hub-admin-bar`/`hub-admin-nav`/`hub-admin-logout` CSS was deleted; a small `Log out` form was nested as an extra item inside `.nav-links` (after the SK badge) rather than added as a third top-level flex child of `.nav-inner`, so the existing `justify-content: space-between` 2-child layout (brand left, everything else right) stays pixel-identical to the public header. Since the public site has no nav/footer link to `/resume/` at all (it's only reachable via Home's "SEE MY RESUME" hero button), the hub's Home page regained that same button (linking to `adminhub:resume`) as Resume's sole navigation entry point, matching the public site's own pattern rather than adding a new footer link the public site doesn't have.
- Reasoning (follow-up): Owner's explicit direction — the whole point of "exactly the same as Public Pages" was a single, faithful header/footer per page, not the public chrome plus a second admin-only strip. Keeping the logout control nested inside the existing right-hand `.nav-links` cluster (instead of as a new sibling of `.nav-inner`) was the only way to add a real, necessary control (there is no auth without a way to log out) without perturbing the header's existing two-column layout math.

## Decision: Resume page upgraded to fully model-backed (`core.ResumePage`, `ResumeExperience`, `ResumeEducation`), independent from About's skill tags

- Status: Accepted
- Date: 2026-08-28 (Phase 11, scope extension)
- Context: The owner asked mid-planning for Resume to be included in the Admin Hub as "totally editable and updatable like other pages." Investigation found only `core.Resume` (the PDF file) was model-backed — the page's Skills & Tools section (3 tag groups) and its Experience/Education entries were still hardcoded placeholder HTML in `templates/pages/resume.html` since Phase 7, with no backing table at all. Two design forks were confirmed directly with the owner: whether Resume's skill tags should be independent fields or literally reuse `About`'s existing `languages`/`frameworks`/`learning` M2M fields (Resume's current hardcoded list already differs in content from About's — e.g. includes Java/JavaScript/HTML/CSS/AJAX not present in About); and whether Experience/Education should become genuine new models with full CRUD (vs. leaving them hardcoded and only wiring up the PDF).
- Decision: The owner chose independent skill fields and full new models for both. Added `core.ResumePage` (singleton, same `.first()` convention as `HeroContent`/`Quote`: `intro_sub`, `languages_label`+`languages` M2M→`projects.Tag` (`related_name="resume_languages"`), `frameworks_label`+`frameworks` (`related_name="resume_frameworks"`), `learning_label`+`learning` (`related_name="resume_learning"`) — deliberately separate `related_name`s and a separate model from `core.About`'s own tag fields, not shared, per the owner's choice), `core.ResumeExperience` and `core.ResumeEducation` (both multi-row, `role`/`organization`/`period`/`description`/`order` and `degree`/`institution`/`period`/`description`/`order` respectively, `Meta.ordering = ["order", "id"]`). Migrations `core/migrations/0027_resumeeducation_resumeexperience_resumepage.py` (schema) and `0028_seed_resume_page.py` (data) seed exactly one `ResumePage` row and today's exact pre-existing placeholder text/tags (2 `ResumeExperience` rows, 1 `ResumeEducation` row) — `Tag.objects.get_or_create()` reused already-existing real `Tag` rows where names overlapped (e.g. `Java`/`Spring Boot` already existed from the TS Library project seed). `core/views.py::resume` now also passes `resume_page`/`experience`/`education`; `templates/pages/resume.html` was converted from hardcoded markup to `{% for %}` loops over these — the one deliberate exception to the standing "Admin Hub is additive, don't touch existing public templates/models" rule for this phase, since genuine editability requires the underlying content to be database-backed in the first place.
- Reasoning: Owner's explicit choice on both forks, given directly when asked. Keeping Resume's skill tags independent (not reusing `About`'s) preserves the two pages' already-different real content without forcing a reconciliation the owner didn't ask for; a dedicated `ResumePage` (rather than adding fields to `core.Resume`, which is specifically "the PDF file" per its existing role) keeps that model's single responsibility intact, mirroring the `Quote`-split-out-of-`HeroContent` precedent.
- Consequences: `core` now holds eleven models total. Resume is no longer an exception to the site's "real content lives in the database" convention — the last page on the site with hardcoded body content is gone. If Resume's skills are ever meant to mirror About's, that would need a fresh decision (not implied by this one). See the Admin Hub decision above for how these three models are edited via `/adminhub/resume/`.

## Decision: Second real Project seeded ("Entry Recorder"); private client repo made public; featured project swapped

- Status: Accepted
- Date: 2026-08-28 (Phase 12)
- Context: The owner asked to formally start Phase 12 by adding a second real project, `C:\Entry Recorder` — a single-user Django/PostgreSQL app built for a client (a manufacturing business, per the app's own `CLAUDE.md`) to replace a paper truck-loading logbook, already deployed live on Render. Three things needed the owner's direct input before content could be written/committed: (1) how much to reveal about the client's business in the public portfolio description; (2) the project's GitHub repo (`RecordEntryApp`) was private, so linking it would either need the repo made public or the link omitted; (3) TS Library was the sole `featured=True` project, and `ARCHITECTURE.md`'s "at most one featured row" invariant meant adding a second real project raised the question of which one Home's featured card should show.
- Decision: The owner chose to (1) keep the client's business generic in the description — referred to only as "a manufacturing client," the specific business name never used; (2) make the `RecordEntryApp` repo public and link it (verified first that no secrets were ever committed — `.env` was never tracked, `SECRET_KEY` is read from the environment, and the only string matches for "password" in tracked files were test-fixture values in `recorder/tests.py`); (3) make Entry Recorder the new featured project, moving TS Library to `featured=False`/`order=2`. The live URL (`https://entryrecorder.onrender.com`, Render's default hostname derived from the `entryrecorder` service name in the client project's own `render.yaml`) was confirmed by an actual HTTP request (200 on `/login/`) rather than assumed from the naming convention alone.
- Reasoning: Owner's explicit choices on all three questions, asked directly via `AskUserQuestion` before any content was written or the repo's visibility was changed (matching this project's established "confirm before acting on someone else's/real-world data" pattern, e.g. the TS Library placeholder-deletion decision above). Verifying the repo was actually secret-free before flipping it to public, rather than just flipping it on the owner's say-so, follows the standing "measure twice" carefulness for hard-to-reverse, externally-visible actions.
- Consequences: `projects/migrations/0012_seed_second_real_project.py` creates the `Project` row (reusing existing `Django`/`Python`/`JavaScript` `Tag` rows, creating one new `PostgreSQL` tag) and, in the same migration, flips TS Library's `featured`/`order` — the two changes are bundled in one migration rather than two, since they're really one atomic "which project is featured" decision. Entry Recorder has no `ProjectImage` rows yet (same `.mockup`-fallback state TS Library shipped in) — real screenshots for both projects remain open Phase 12 work. If a third real project is added later, the same three questions (client-detail disclosure, repo visibility, featured-slot ownership) should be confirmed the same way rather than assumed from this entry's precedent. **`live_url` removed the same day — see the next decision.**

## Decision: Entry Recorder's `live_url` removed — the site is login-gated, so a public Live URL button has no value to a visitor

- Status: Accepted
- Date: 2026-08-28 (Phase 12, same-day follow-up)
- Context: Unlike TS Library (publicly browsable once live), Entry Recorder is a single-user app — anyone reaching `https://entryrecorder.onrender.com` hits a login wall with no way in. The owner pointed out that showing a "Live" button/link for a site a portfolio visitor can never actually enter is pointless.
- Decision: `projects/migrations/0013_entry_recorder_remove_live_url.py` clears `Project.live_url` on the Entry Recorder row (reversible — the reverse migration restores the URL) rather than deleting the field or the row. The GitHub link stays. No template change was needed — `project_detail.html`/the projects-list card/modal already guard the Live-URL button with an independent `{% if project.live_url %}` (Phase Y), so an empty `live_url` makes it disappear automatically, exactly the same mechanism that already handles a project with no GitHub URL either.
- Reasoning: Owner's explicit direction. Clearing the field via a data migration (not deleting the `Project.live_url` field itself) keeps the fix scoped to this one project — the field remains meaningful and populated for any future publicly-browsable project (as TS Library already demonstrates).
- Consequences: Entry Recorder's detail page, list card, and modal now show only a GitHub button. The long-form `description` field's prose (which still says things like "deployed and live on Render") is unaffected and intentionally left as-is — the app genuinely is deployed and live; only the clickable public Live-URL affordance was removed, since that specific UI element implied "you can visit and try this," which isn't true here. If a project is ever both deployed and login-gated again, the same pattern (populate `github_url`, leave `live_url` blank) applies without needing to ask again.

## Decision: "Entry Recorder" `Project` row renamed to "LoadGate"

- Status: Accepted
- Date: 2026-08-28 (Phase 12, same-day follow-up)
- Context: The owner asked for the project's portfolio title to be changed from "Entry Recorder" to "LoadGate." (The client project's own `CLAUDE.md` already showed "LoadGate_Entries" as the cosmetic Render dashboard/Blueprint name it was deployed under, so this brings the portfolio entry's title in line with the name the owner actually uses for the product, not an arbitrary relabel.)
- Decision: `projects/migrations/0014_entry_recorder_rename_loadgate.py` updates the row's `title` (`"Entry Recorder"` → `"LoadGate"`) and `slug` (`entry-recorder` → `loadgate`), and does a targeted `str.replace()` of the old name for the new one inside the two places `description`'s prose names the project (its opening heading line and first sentence) — the rest of the long-form write-up, and every other field (`short_description`, `github_url`, tags, `category`/`role`/`year`/`status`), was left untouched, since none of them name the project by title. Reversible — the reverse migration restores both the old title/slug and reverts the two description mentions.
- Reasoning: Owner's explicit direction. Renaming the `slug` alongside the `title` (not just the display title) was a judgment call, not explicitly asked for — done to keep the project's URL (`/projects/loadgate/`) consistent with its new displayed name, matching how every other seeded `Project`'s slug already matches its title (e.g. `ts-library` for "TS Library"); low-risk since the old slug had no external backlinks yet (git history/no public sharing of `/projects/entry-recorder/` to date).
- Consequences: `/projects/entry-recorder/` (the pre-rename detail URL) now 404s; `/projects/loadgate/` is the live one. If this project is ever renamed again, or if a slug is ever renamed after being publicly shared/linked elsewhere, a redirect (not just a slug swap) would need to be considered — not needed here since this project's URLs were never distributed externally.

## Decision: LoadGate's first `ProjectImage` — a real dashboard screenshot, checked for real client data before use

- Status: Accepted
- Date: 2026-08-28 (Phase 12, same-day follow-up)
- Context: The owner supplied a screenshot of the client app's real Truck Entries dashboard to use as LoadGate's primary preview image. A first screenshot supplied earlier in the same conversation was flagged and not used — it showed real vehicle plate numbers, real load weights, and a real staff member's name ("Welcome, Kalim"), which conflicted with the owner's earlier "keep the client generic" instruction on this same project. A second screenshot was then supplied.
- Decision: Before seeding it, the second screenshot's vehicle numbers (e.g. `MH66HLW6527`, `HR20OA4002`) were checked against `recorder/management/commands/seed_entries.py`'s `random_vehicle_number()` generator in the client project (`C:\Entry Recorder`) and matched its exact format (state code + 1–2 digit RTO + 1–3 random letters + 4-digit number) — confirming the visible entries are synthetic dev-seed data, not real client records. The header also read "Welcome, Admin," this session's own just-reset local dev credentials (see the separate local-dev credential reset earlier the same day), not a real staff name. With both concerns cleared, it was seeded as `projects.ProjectImage` (`order=0`, caption "Truck loading entries dashboard") via the ORM directly — not a data migration, matching the established convention that `ProjectImage`/`Resume`/`ContactRequest`/`HireRequest` are populated by real action (an upload, a submission), never seeded content.
- Reasoning: The same "verify before publishing something that could expose someone else's real data" discipline already applied to the GitHub-repo-secrets check before making `RecordEntryApp` public — a claim that data "looks fake" isn't enough when the underlying generator's exact format can be checked directly, so it was checked rather than assumed.
- Consequences: LoadGate now shows a real product screenshot (not the `.mockup` fallback) on Home's featured card, both Projects-list cards, and the detail-page gallery. The image itself still shows the client app's own "Entry Recorder" sidebar branding (the client app has not been renamed to match the portfolio's "LoadGate" title, and there's no reason it should be — they're independently named) — not a bug, just worth knowing if the mismatch is ever noticed. If more LoadGate screenshots are added later, apply the same real-data check first — don't assume a later screenshot is automatically as safe as this one.

## Decision: Blog `Post`/`Tag` relocated to `projects`; every tag-shaped table across `core`/`projects` converted to a shared `Tag` model

- Status: Accepted
- Date: 2026-08-26
- Context: Building the real Blog feature (upgrading Phase - X beyond Phase 7's static placeholder) required deciding where the new table lives and how tags are modeled. Three things were confirmed directly with the owner, in sequence, within the same session: (1) the new Blog table goes in `projects`, not `core` — overriding the "Feature placement — Designs, Blog, Resume, Timeline, Hire Me → `core`" decision above, which had scoped `projects` "strictly to the owner's projects/works/code — nothing else"; (2) tags for the new table use a real relational `Tag` model (not a comma-separated `CharField`), with an enforced 1–6 count; (3) after a mid-task follow-up ("use Real Tag model for all the tables where tags used"), clarified to mean every table — in both `core` and `projects` — that uses tags, not just the new one. A grep across every template for tag-list-style loops found exactly two other tag-shaped fields in the codebase: `projects.Project.tags` (comma `CharField` + `tag_list()`) and `core.About.languages`/`.frameworks`/`.learning` (three comma `CharField`s + `_list()` methods, which `ARCHITECTURE.md` already documented as an explicit reuse of `Project.tag_list()`'s pattern). `templates/pages/resume.html`'s Skills section was identified and explicitly excluded — it's hardcoded static markup with no backing table at all (a deliberate Phase 7 choice), so "every table that uses tags" doesn't reach it.
- Decision: `projects` gains a shared `Tag` model (`name`, unique) and a `Post` model (`title`, `slug`, `short_description`, `content`, `tags` — `ManyToManyField(Tag, related_name="posts")`, `published_at`), plus `blog_list`/`blog_detail` views and `projects/blog_urls.py`, `include()`d from `core.urls` at `/blog/` (url name `blog` preserved for the footer link; `blog_detail` new). `core.views.blog` (the old static placeholder) was removed. The 1–6 tag count is enforced via a custom `PostAdminForm.clean_tags()` in `projects/admin.py` (a `ManyToManyField`'s cardinality can't be validated by `Model.full_clean()` or a DB constraint, since the M2M through-row needs the instance's PK — a `ModelForm.clean_<field>()` is the correct mechanism) — the field's lower bound is additionally covered by Django's own default "this field is required" validation since `blank=True` was not set. `projects.Project.tags` was converted from its comma-`CharField` to `ManyToManyField(Tag, related_name="projects")` via a safe multi-step migration (temp field name → data migration → remove old field → rename) — no upper-bound count constraint was added here, only to the new `Post` table, since that's what the owner asked for. `core.About.languages`/`.frameworks`/`.learning` were converted the same way, as three separate M2M fields (not one field, not a category-flagged through-model) to `projects.Tag`, preserving the page's existing three-section structure exactly; `core/models.py` now has a cross-app import (`from projects.models import Tag`), mirroring the precedent `core/views.py` already set by importing `Project` from `projects.models`. `Project.tag_list()`/`About.languages_list()`/`.frameworks_list()`/`.learning_list()` were removed; templates now call `.tags.all` / `.languages.all` / `.frameworks.all` / `.learning.all` directly.
- Reasoning: Owner's explicit direction for all three points, given directly when asked/clarified during this session. Only Blog moves out of `core` — Designs (unbuilt), Resume, and Hire Me stay, per the original 2026-08-24 decision. Converting `Project`/`About`'s existing tag-shaped fields to the same `Tag` model (rather than leaving them on the old comma-`CharField` pattern) gives the site one shared tag vocabulary instead of three independent, non-relational copies of the same idea.
- Consequences: The "`projects` is scoped strictly to the owner's projects/works/code — nothing else" language in the 2026-08-24 decision (and echoed in `ARCHITECTURE.md`) is no longer fully accurate — `projects` now also hosts Blog content. `Post.tags`' 1–6 rule is enforced only at the admin-form layer, not the database — any future code path creating/editing `Post.tags` outside the admin (a script, `manage.py shell`, a future public submission form) won't get this validation automatically; noted as an Important Invariant in `ARCHITECTURE.md`. The three seeded placeholder `Project` rows all stored the literal text `"Tag, Tag, Tag"` — splitting that on comma and de-duplicating into a real set collapsed each project down to a single shared `Tag` named "Tag" (a set can't hold duplicate members); each placeholder project now shows 1 tag chip instead of 3. Not a bug — a correct consequence of moving generic placeholder text into a real relational set; the owner can re-tag via `/admin/` any time, and real project tagging is Phase 12's job anyway. `About`'s real skill-tag content (12 distinct real words across the three groups) was preserved exactly through the conversion — verified word-for-word post-migration — but now renders alphabetically (`Tag.Meta.ordering = ["name"]`) rather than in its original hand-authored order, a minor, accepted display-order change. If Designs is ever placed in `projects` too, that needs its own fresh decision — this entry only covers Blog and the tag-model conversion.

## Decision: Phase 15 codebase deploy-readiness — gunicorn + whitenoise + Cloudinary (media), `render.yaml` Blueprint

- Status: Accepted
- Date: 2026-09-03
- Context: Phase 15's stack (Render + Neon + UptimeRobot) was decided back in Phase 10, but no code existed yet to actually run this project in production — no WSGI server, no static-file pipeline, no production security settings, and critically: **Render's free tier has no persistent disk** (confirmed directly — persistent disks are a paid-only feature, $0.25/GB/mo). Since `MEDIA_ROOT` currently holds real content (the Resume PDF, every `ProjectImage` screenshot across three real seeded projects), deploying as-is would silently wipe all of it on the very next redeploy. A second hosting option the owner found, `onehostmarket.com` (a budget cPanel/LiteSpeed reseller), was evaluated and set aside — no knowledge base or docs existed to confirm Python/WSGI app support, no PostgreSQL was mentioned, and it's a paid plan (₹39/mo) rather than free. Owner's call: proceed with the original Render/Neon/UptimeRobot plan.
- Decision: Made the codebase itself deploy-ready:
  - **WSGI server:** `gunicorn` added to `requirements.txt`; `render.yaml`'s start command is `gunicorn config.wsgi:application`.
  - **Static files:** `whitenoise` added to `MIDDLEWARE` (right after `SecurityMiddleware`) and to `STORAGES['staticfiles']` — `whitenoise.storage.CompressedManifestStaticFilesStorage` when `DEBUG=False`, plain `django.contrib.staticfiles.storage.StaticFilesStorage` when `DEBUG=True` (branched so local dev never needs a `collectstatic` run just to resolve `{% static %}` tags). New `STATIC_ROOT = BASE_DIR / 'staticfiles'` (gitignored) is `collectstatic`'s target.
  - **Media storage — went through two providers the same day.** First attempt was **Cloudflare R2** (`django-storages`+`boto3`, `S3Storage`), but activating R2 at all requires a payment method on file (charges only past the free limits, but the card is mandatory up front) — the same card blocker that already ruled out Oracle Cloud (see Phase 10's stack decision). Confirmed live in Cloudflare's dashboard (the "Add R2 subscription to my account" step), not assumed. Switched same-session to **Cloudinary** instead: `cloudinary`+`django-cloudinary-storage`, no card required for its free tier (25 credits/month — 1 credit ≈ 1GB storage/bandwidth/1K transformations), confirmed via its own pricing page before signing up. `STORAGES['default']` branches on whether `CLOUDINARY_CLOUD_NAME` is set in the environment — if set, uses `cloudinary_storage.storage.MediaCloudinaryStorage` (`CLOUDINARY_STORAGE` dict sourced from `CLOUDINARY_CLOUD_NAME`/`CLOUDINARY_API_KEY`/`CLOUDINARY_API_SECRET`); if unset, falls back to Django's plain `FileSystemStorage` writing to the existing `MEDIA_ROOT` — the same env-presence-gated fallback shape Phase 10's `DATABASE_URL` branch established. `INSTALLED_APPS` gained `cloudinary_storage` (before `django.contrib.staticfiles`) and `cloudinary` (after), per the package's own documented ordering requirement.
  - **Resume PDF needed a second storage class.** `MediaCloudinaryStorage` forces `resource_type='image'` — the first connectivity test (a plain `.txt` file) failed with `cloudinary.exceptions.BadRequest: Invalid image file`, confirming this the hard way rather than assuming it. `core.Resume.file` (the only non-image upload in the project — a PDF) now uses a dedicated `resume_storage()` callable (`core/models.py`) that returns `RawMediaCloudinaryStorage()` when Cloudinary is configured, or `django.core.files.storage.default_storage` otherwise — same env-presence branch, applied per-field via `FileField(storage=...)` since Django 4.2 allows a callable there (must stay a module-level function, not a lambda, so migrations can serialize it by reference). Required a new migration (`core/migrations/0031_alter_resume_file.py`) since the field's `storage` argument changed. `HeroContent.portrait` and `ProjectImage.image` (both `ImageField`) are unaffected — they use the default (image) storage correctly as-is.
  - **A dedicated Cloudinary API key** (`portfolio-render`, Master Admin role) was created rather than reusing the account's existing `Root` key, isolating this project's credential the same way Phase 10 created a dedicated second Neon project rather than reusing the owner's existing one — email-verified via a one-time code (Cloudinary requires this for new admin-level keys).
  - **Production security settings**, all gated on `not DEBUG` (or reading a new env var): `CSRF_TRUSTED_ORIGINS` (from `DJANGO_CSRF_TRUSTED_ORIGINS`), `SECURE_PROXY_SSL_HEADER` (Render terminates TLS at a proxy; without this `request.is_secure()` is always `False` in production), `SESSION_COOKIE_SECURE`/`CSRF_COOKIE_SECURE`, and `SECURE_SSL_REDIRECT`/`SECURE_HSTS_SECONDS` (3600s starting value)/`SECURE_HSTS_INCLUDE_SUBDOMAINS`/`SECURE_HSTS_PRELOAD`.
  - **`render.yaml`** (Blueprint spec) added at the repo root: build command (`pip install -r requirements.txt && collectstatic && migrate`), start command, `PYTHON_VERSION` (3.11.9, matching the new `.python-version`), an auto-generated `DJANGO_SECRET_KEY`, `DJANGO_DEBUG=False`, and every other env var (`DJANGO_ALLOWED_HOSTS`, `DJANGO_CSRF_TRUSTED_ORIGINS`, `DATABASE_URL`, the three `CLOUDINARY_*` keys) marked `sync: false` — named placeholders only, no real values committed.
- Reasoning: Minimal-dependency preference (Phase 9's `python-dotenv` precedent) ruled out heavier alternatives in favor of the smallest packages solving each problem directly. Cloudinary over R2 purely on the card requirement — not a technical shortcoming of R2, which remains S3-compatible and would otherwise have been the simpler single-storage-class choice (Cloudinary's image/raw split is the one added wrinkle it introduces). A dedicated Cloudinary key over the account's Root key follows the isolation precedent already set for Neon.
- Consequences: `manage.py check` and a simulated production `manage.py check --deploy` both run clean (only the pre-existing, intentional `X_FRAME_OPTIONS=SAMEORIGIN` warning remains). `manage.py collectstatic` was verified end-to-end against the real Tailwind-built `static/` tree (131 files, 387 post-processed, no errors). Cloudinary connectivity was verified with a real upload/delete round-trip through *both* storage classes (an actual PNG through `MediaCloudinaryStorage`, a text file through `RawMediaCloudinaryStorage`), confirmed via Cloudinary's Admin API directly (not just the Django-level `.exists()` check, which this package's storage backend doesn't implement meaningfully — it returns stale/incorrect results by design, always allowing Django to proceed with an upload rather than blocking on a false collision) — both test objects confirmed actually removed after `.delete()`, no leftover clutter in the account's real media library. Local `.env` keeps the three `CLOUDINARY_*` values commented out (same pattern as Neon's `DATABASE_URL`) so day-to-day local dev keeps writing to disk; only Render's own env vars activate Cloudinary in production. This closes out the *codebase* side of Phase 15 — the account-level side (creating the Render service from this Blueprint, pasting the Neon `DATABASE_URL` and the three `CLOUDINARY_*` values into Render, the first real deploy, DNS/hostname, and the UptimeRobot monitor) still requires the owner's own accounts/credentials and hasn't happened yet. See `PHASES.md`'s Phase 15 entry and `TASKS.md` for exactly what's left.

## Decision: Render service named `shahbazkhan`; first live deploy — three real bugs found and fixed, plus a Neon data gap

- Status: Accepted
- Date: 2026-09-03
- Context: With the codebase deploy-ready (previous decision), the actual Render web service still needed creating. First question: what to name it, since the service name sets the `*.onrender.com` subdomain. A web search on portfolio-site domain-naming conventions confirmed **firstname+lastname** is the standard, uncomplicated choice for a personal portfolio (vs. a brand name, more suited to a product/company) — so the service was named `shahbazkhan`, changed from an initial placeholder `portfolio` (`render.yaml`'s `name:` key, committed and pushed before creating the service so the Blueprint would pick up the real name). The Render Blueprint was then created via the dashboard (GitHub repo already connected, no new auth needed), with `DJANGO_ALLOWED_HOSTS`/`DJANGO_CSRF_TRUSTED_ORIGINS` pre-filled as `shahbazkhan.onrender.com` / `https://shahbazkhan.onrender.com` (guessed ahead of the actual assignment — confirmed correct once deployed, Render does assign the exact Blueprint-specified name when available) and all other `sync: false` values (the Neon `DATABASE_URL`, the three `CLOUDINARY_*` values) filled in from what Phase 10/the previous decision had already produced.
- Decision: The first deploy attempt failed at the build step; three real, sequential bugs were found and fixed, each verified locally before pushing again and each triggering a fresh auto-deploy:
  1. **`AttributeError: 'Settings' object has no attribute 'STATICFILES_STORAGE'`** — `django-cloudinary-storage`'s own `collectstatic` management-command override reads that legacy attribute directly rather than the `STORAGES` dict this project uses exclusively (Django 4.2+ style, never set the old-style setting). Fixed by also setting `STATICFILES_STORAGE = STORAGES['staticfiles']['BACKEND']` in `config/settings.py` for the package's benefit — Django itself still governs behavior via `STORAGES`, this attribute exists only for backward-compat readers.
  2. **`whitenoise.storage.MissingFileError` for `admin/img/sorting-icons.svg`**, `collectstatic` stopping at 76/127 admin static files. Root cause: Django resolves same-named management commands by *first* `INSTALLED_APPS` entry wins (`django/core/management/__init__.py`'s `get_commands()` iterates `reversed(apps.get_app_configs())`, so the first-listed app's registration survives last) — with `cloudinary_storage` listed before `django.contrib.staticfiles` (the package's own documented convention, needed only if you want *static* files routed to Cloudinary too), its own `collectstatic` override was winning and only partially, incorrectly collecting files. Since this project only needs Cloudinary for *media*, not static files, `INSTALLED_APPS` was reordered (`django.contrib.staticfiles` before `cloudinary_storage`) so Django's real `collectstatic` (and `whitenoise`'s manifest storage) wins instead, bypassing the override entirely — verified via a direct read of Django's own command-resolution source before making the change, not assumed.
  3. **Live homepage 500: `ValueError: Missing staticfiles manifest entry for 'css/main.css'`.** `static/css/main.css` is gitignored (built locally via `npm run build:css`, never committed — `CLAUDE.md` already documented "`runserver` does not run this automatically") and `render.yaml`'s `buildCommand` never ran the Tailwind build at all, only the Python steps. Fixed by prepending `npm install && npm run build:css` to `buildCommand` — Render's Python-runtime build image includes Node/npm, confirmed by this actually working, not assumed in advance.
  - **Separately, a real data gap** (not a code bug): once the CSS was fixed, the homepage rendered but the hero portrait, all three project screenshots, and (implicitly) the Resume PDF were missing — `Resume.objects.count()` and `ProjectImage.objects.count()` were both **0 on Neon**, discovered by querying directly rather than assumed. Every prior "uploaded via the ORM/admin, not seeded via migration" media action in this project's history (LoadGate's and TS Library's and Fire Service's screenshots, the Resume PDF) was performed against whichever database was active *in that session* — which, post-Phase-10, has been the *local* Postgres install for day-to-day dev, not Neon. Data migrations (schema + seeded placeholder rows) apply identically to any database `migrate` runs against, so Neon had all the migration-seeded content correct; only the ORM-uploaded rows were missing, since those were never part of a migration. Fixed with a one-time, uncommitted script (matching the established "admin-uploaded, not a migration" convention for this kind of content) that read each real local file from this machine's `media/` folder, uploaded it to Cloudinary, and created the equivalent `Resume`/`ProjectImage` rows directly against Neon (`DATABASE_URL` pointed at Neon, Cloudinary env vars active, run as a standalone script rather than piped into `manage.py shell`, whose interactive REPL was found to mangle multi-line blocks fed via stdin — confirmed by the loop silently processing zero rows on the first attempt). One already-existing `HeroContent.portrait` reference (from an earlier, accidental one-off re-upload attempt) was left as its already-migrated Cloudinary path rather than re-touched.
- Reasoning: Every fix was diagnosed from the actual Render build/runtime logs and Django/package source, not guessed — the command-resolution behavior (first-app-wins) was confirmed by reading `django/core/management/__init__.py` directly before reordering `INSTALLED_APPS`, and the Neon data gap was confirmed by directly querying row counts on both databases before writing the backfill script, rather than assuming the cause.
- Consequences: The live site (`https://shahbazkhan.onrender.com`) was verified in-browser after all four fixes — correct Tailwind styling, the real hero portrait and SK nav badge, and all three real project screenshots (TS Library, LoadGate, Fire Service Website) rendering from Cloudinary on `/` and `/projects/`. Two Cloudinary artifacts are now orphaned-but-harmless (an unused duplicate hero-portrait upload from the accidental first re-upload attempt, and the very first `deploy-check` test files from the earlier connectivity test, already cleaned up at the time). Going forward, any *new* media uploaded via Admin Hub against the *local* dev database will **not** automatically appear on the live Neon-backed site — that's expected (each environment's database is independent, per Phase 10's design), but worth remembering: real content intended for the live site should be uploaded through `/adminhub/` on the production site itself (or explicitly re-run through a script like this one), not assumed to sync from local dev. UptimeRobot (Phase 15's last remaining item) is not yet set up — see `TASKS.md`.

## Decision: Projects page filter/search/pagination converted to AJAX — no page reload, no URL change

- Status: Accepted
- Date: 2026-09-03 (Phase 16, first concrete slice)
- Context: Phase 16 "AJAX Across the Site" was logged the same day Phase 15 closed, with its scope explicitly left open ("which pages/interactions convert first" unresolved). The owner asked to start with the Projects list page (`/projects/`): category/tag/search filtering and pagination/clear-filters should update the results in place, with a hard, explicit constraint — no full page reload, and the browser's address bar must not change at all (not even via `history.pushState`/`replaceState` to a non-navigating state). Confirmed directly with the owner (`AskUserQuestion`) on three sub-decisions: the search box filters live as-you-type (debounced), the category `<select>` is included in the same AJAX path (not left as a full reload), and pagination/"Clear filters" are also included, so the whole filter surface behaves consistently.
- Decision: `projects/views.py::project_list` now serves two response shapes from the same view/URL, chosen by a `X-Requested-With: XMLHttpRequest` header the client JS sets manually on its `fetch()` calls (a header-based content-negotiation convention, not a new dependency) — `partials/project_results.html` (a new file, the cards/modal-templates loop plus the pagination section, extracted verbatim from `pages/projects.html`) for AJAX calls, `pages/projects.html` (now `{% include %}`ing that same partial inside a `<div id="proj-results">`) for normal page loads. The context dict passed to `render()` is identical either way — no filtering-logic duplication. The queryset also gained `.prefetch_related("tags", "images")`, since this endpoint is now hit far more often (once per debounced keystroke). All new client-side logic lives in `projects.html`'s existing `extra_body` `<script>`, extending (not replacing) the Phase 8 modal IIFE: a `state` object (`category`/`tag`/`q`/`page`) seeded from new `data-initial-*` attributes on `#proj-results` (mirroring the existing `data-modal-trigger`/`data-nav-toggle` attribute convention, since there was no prior pattern for passing server data into JS at all); a `loadResults()` function using `AbortController` to cancel a superseded in-flight request (race-safety for fast typing); a ~350ms debounce on the search input only (category/tag/pagination/clear fire immediately); and event delegation on `#proj-results` for the modal-open triggers and pagination links specifically, since those two live *inside* the swapped region and the pre-existing direct-listener-at-load approach would not survive a DOM replacement — tag chips, the category select, and the search form all sit *outside* `#proj-results` (in `.proj-filters-section`) and keep their original direct listeners, since they're never replaced. The URL never changes because nothing calls `history.pushState`/`replaceState` or touches `window.location` beyond reading `.pathname` to build the fetch target. The original `<form method="get">`/`<a href="?...">` markup was left completely intact (JS only adds `preventDefault()` on top), so a no-JS visit or a direct `?q=...&category=...&tag=...&page=...` link still gets a correct full server-rendered page through the exact same `project_list` code path — matching the same progressive-enhancement layering the Phase 8 project-detail modal already established. One new CSS rule, `.proj-results-loading{ opacity:.5; pointer-events:none; }`, was added for a loading affordance; every other class was reused verbatim.
- Reasoning: Owner's explicit choices on all three sub-decisions, given directly when asked, matching this project's established "the owner confirms scope via direct questions" pattern (e.g. the Phase Y three-question round). Header-based negotiation on the same view/URL was chosen over a separate JSON endpoint or a new URL specifically to avoid duplicating the filtering logic and to keep a single source of truth, consistent with the project's repeated minimal-surface-area preference (e.g. the Phase 9 `python-dotenv`/`LocMemCache` choices, the Phase 15 Cloudinary-over-R2 choice). `AbortController` over a monotonic-counter guard for race safety is simpler and needs no extra bookkeeping. Event delegation only where structurally required (the swapped region) rather than everywhere, to keep the change as close as possible to the existing direct-listener convention used elsewhere in the codebase.
- Consequences: This is the first real client→server AJAX interaction anywhere in this codebase (previously, "dynamic" meant a pure client-side `hidden`-attribute toggle with zero server round-trips — the Phase 8 project modal, the Phase 13 nav drawer, the resume-preview modal). The explicit tradeoff of "no URL change," by the owner's own request: filtered/paginated Projects-page states are no longer bookmarkable or shareable via URL, and the browser back button no longer steps through filter history — it leaves `/projects/` entirely, since no history entries are ever created for these interactions. The no-JS/direct-link fallback is unaffected and still fully functional (verified). If a future page (Blog's list view is the most likely next candidate, per `ARCHITECTURE.md`'s Important Invariants noting `project_list`'s filter shape as the template for any future list page) gets the same treatment, this decision's header-based-negotiation-plus-partial-template pattern, event-delegation-only-where-swapped rule, and `data-initial-*`-attribute state-seeding convention should be reused rather than reinvented. `Post.tags`'s own list page (`blog_list`) is still unaffected — a bare `.all()`, no AJAX, no filtering at all yet.
- Follow-up fix (2026-09-03, same day): the "Clear filters" link was found to disappear permanently after this change — it was only rendered into the DOM at all when a filter was already active on the initial page load (`{% if query or selected_category or selected_tag %}`), so on a normal unfiltered visit the JS's one-time `querySelector(".proj-filter-clear")` returned `null` and the link could never be shown later no matter what the user filtered by (the filter bar sits outside the AJAX-swapped `#proj-results`, so it's never re-rendered by the server after the initial load). Fixed by always rendering the link and toggling visibility with the `hidden` attribute (`{% if not query and not selected_category and not selected_tag %}hidden{% endif %}`) instead of `{% if %}`-omitting the element — matching the site's existing `hidden`-attribute-toggle convention rather than a template-conditional-existence one. General lesson for any future AJAX work on this site: an element that must be toggled by client-side state after page load has to exist in the initial DOM (even if hidden), not be conditionally rendered into or out of existence server-side.

## Decision: Contact/Hire Me forms converted to AJAX submission — no full page reload

- Status: Accepted
- Date: 2026-09-03 (Phase 16, second slice)
- Context: Following the Projects-page AJAX conversion above, the owner asked for the same no-reload treatment on the Contact and Hire Me pages' "Send message"/"Send request" forms specifically — clicking submit should not refresh the whole page. Both pages already shared one view helper, `core._handle_lead_form(request, form_class, template_name)` (Phase 9), implementing a POST/redirect(`?sent=1`)/GET (PRG) pattern — a real full-page reload/redirect on every submission, valid or not.
- Decision: Extended the same header-based content-negotiation pattern used for the Projects page. `_handle_lead_form` gained a `partial_template_name` parameter and now branches on `request.headers.get("X-Requested-With") == "XMLHttpRequest"`: a non-AJAX POST keeps its exact original behavior (invalid → re-render `template_name` with errors, valid → `redirect(f"{request.path}?sent=1")`, unchanged for no-JS visitors); an AJAX POST never redirects — it renders the new partial (`partials/contact_form.html` / `partials/hire_form.html`, each extracted verbatim from the corresponding page's existing `{% if sent %}...{% else %}<form>...{% endif %}` block) directly, with `sent=True` and a fresh blank form instance on success, or `sent=False` and the same bound, error-carrying form on failure — both cases return HTTP 200, since the partial's own content (success message vs. re-shown form-with-errors) is what tells the client which happened, not the status code. `contact.html`/`hire.html` now wrap that block in `<div id="contact-form-wrap">`/`<div id="hire-form-wrap">` and `{% include %}` the partial. Each page's `extra_body` script listens for `submit` on the wrap div itself (not the `<form>` directly) — since `submit` bubbles and the wrap div is never replaced (only its contents are, on every AJAX response), this single delegated listener survives the form being swapped out and back in after an error round-trip, the same "delegate on the container that persists, not the content that gets swapped" rule established by the Projects page's pagination/modal-trigger handling. The listener does `event.preventDefault()`, POSTs `new FormData(form)` (this automatically carries the CSRF token via the form's existing `{% csrf_token %}` hidden input — no separate `X-CSRFToken` header needed) with the `X-Requested-With` header, and replaces the wrap's `innerHTML` with the response text. A network-failure `.catch()` inserts a `.form-errors.ajax-form-error` banner at the top of the *existing* form (reusing the already-styled `.form-errors` class) rather than wiping the user's typed input, and re-enables the submit button — deliberately less destructive than the Projects page's "replace the whole region" error handling, since a filter query costs nothing to redo but a half-written message/request does.
- Reasoning: Reusing the exact "one view, two response shapes via header negotiation" pattern from the Projects-page decision (rather than inventing a JSON API or a second URL) keeps both AJAX conversions consistent and keeps `_handle_lead_form`'s single source of truth for validation/save logic intact — exactly the kind of reuse `ARCHITECTURE.md`'s Important Invariants already calls out as the precedent to follow. Submitting via `FormData` rather than hand-building a URL-encoded body or JSON avoids re-deriving Django's CSRF handling — the existing hidden input already does the job. Preserving user input on a network error (rather than the Projects page's swap-and-replace approach) reflects that the two features have different failure costs.
- Consequences: Contact/Hire Me submissions no longer redirect or reload for JS-enabled visitors — the `?sent=1` URL suffix is now AJAX-path-only-avoided (never appears), matching the Projects page's own "no URL change" outcome even though the owner didn't repeat that specific instruction this time (it falls out naturally from never calling `redirect()` on the AJAX path). The no-JS/PRG fallback is completely unchanged and was verified still working (a plain POST without the header still 302s to `?sent=1`). Both a valid and an invalid AJAX submission were verified end-to-end via direct POST (real `ContactRequest`/`HireRequest` rows confirmed saved then removed as test data; an invalid submission confirmed returning the same field-level `aria-invalid`/`role="alert"` error markup Phase 9 already established, now via the partial instead of a full page). Any future lead-capture-style form on this site should follow this same "extend `_handle_lead_form`, extract a matching partial, delegate the `submit` listener on a persistent wrapper" shape rather than reinventing the mechanism.
