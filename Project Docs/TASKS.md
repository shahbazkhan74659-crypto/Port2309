# Current Tasks

## Active

None actively in progress. [Phase 17] "SEO Optimization" and [Phase 18] "Public Feedback / Testimonials Page" are both complete **and confirmed live in production** (deployed 2026-09-04 — see the Next section below for how).

## Next

If a further page/interaction ever wants the same AJAX treatment Phase 16 established (Projects page filtering/pagination, Contact/Hire Me form submission), that would be new, separately-scoped work — not resumed Phase 16 work. One candidate already flagged in `ARCHITECTURE.md`'s Important Invariants if this comes up again: Blog's list page (`blog_list`) has no filter/search/pagination logic at all yet (unlike Projects), so it isn't a like-for-like AJAX candidate until/unless that's built first.

Resolved 2026-09-04: [Phase 17]'s and [Phase 18]'s production deploys are both done — pushing Phase 18's work to `main` triggered a Render auto-deploy, and since `render.yaml`'s `buildCommand` runs `python manage.py migrate` on every build, both phases' pending migrations (`0032_local_seo_copy.py` and `0033_feedback.py`) applied to Neon in that same deploy. Confirmed live via `curl` against `https://shahbazkhan.onrender.com`: `/about/` shows the real local-SEO copy (Phase 17), `/feedback/` returns 200 with the correct page (Phase 18, testimonial grid empty as expected — Neon's `Feedback` table has zero rows, since local dev's test submissions never sync there, per the standing note below), and `/adminhub/feedback/` correctly redirects anonymous visitors to login.

Resolved 2026-09-04: [Phase 17] "SEO Optimization" is complete — favicon, `robots.txt`, `sitemap.xml`, canonical URLs, Open Graph/Twitter tags, JSON-LD `Person` structured data (with all six target locations), and owner-approved local-SEO copy on the About/Hire Me pages. See `PHASES.md` and `DECISIONS.md` for full implementation detail, and Completed below.

Resolved 2026-09-04: the roadmap was extended with [Phase 17] "SEO Optimization" and [Phase 18] "Public Feedback / Testimonials Page" at the owner's request — see `PHASES.md`, `DECISIONS.md`.

Resolved 2026-09-03: [Phase 16] "AJAX Across the Site" is complete — owner's explicit call to close the phase at two delivered slices rather than expand it further. See `PHASES.md` and `DECISIONS.md`.

Resolved 2026-09-03: [Phase 16] second slice — the Contact and Hire Me pages' "Send message"/"Send request" forms now submit via `fetch()` with no full page reload (and, as a natural consequence of never calling `redirect()` on the AJAX path, no `?sent=1` URL change either). See `DECISIONS.md`'s "Contact/Hire Me forms converted to AJAX submission" entry for the full implementation (the shared `core._handle_lead_form` helper extended with the same header-based negotiation as the Projects page, new `partials/contact_form.html`/`hire_form.html`, `submit` listener delegated on the persistent wrap div since the `<form>` itself gets replaced on every response). The no-JS/PRG fallback (a plain POST without the AJAX header still redirects to `?sent=1`) is unaffected and was verified still working; both a valid and an invalid AJAX submission were verified end-to-end (real DB rows confirmed saved then removed as test data).

Resolved 2026-09-03: [Phase 16] first slice — the Projects page (`/projects/`) now filters (search box, tag chips, category dropdown) and paginates/clears filters entirely via `fetch()`, with no full page reload and no browser URL change at all, per the owner's explicit requirement. See `DECISIONS.md`'s "Projects page filter/search/pagination converted to AJAX" entry for the full implementation (header-based content negotiation on the existing `project_list` view/URL, a new `partials/project_results.html`, `AbortController`-based race safety, debounced live search, event delegation for the modal-open triggers and pagination links). The existing no-JS/direct-link fallback and the project-detail modal are both unaffected and were verified still working. A same-day follow-up fixed a bug where the "Clear filters" link could never appear after filtering from an initially-unfiltered page load (it was `{% if %}`-omitted from the DOM entirely rather than hidden-and-toggleable) — see `DECISIONS.md`.

The live site is at `https://shahbazkhan.onrender.com`, monitored by UptimeRobot, connected to Neon + Cloudinary. See `DECISIONS.md`'s "Render service named `shahbazkhan`; first live deploy" entry for the bugs found and fixed getting there.

**Note for future admin/content work:** uploading new media via `/adminhub/` against the *local* dev database does **not** automatically appear on the live site — Neon (production) and local Postgres are independent databases. New real content meant for the live site should go through `/adminhub/` on the production site itself.

Additional real blog posts and real projects can be authored/seeded anytime — not gated on any phase.

One follow-up open from [Phase 13]: a full interactive, in-browser click-through of every Admin Hub form page at mobile width (`project_form.html`, `post_form.html`, etc.) hasn't been done — static/CSS review found no other fixed-width hazards beyond the one already fixed, but wasn't visually confirmed. Not blocking, can be picked up anytime.

Resolved 2026-09-03: [Phase 13] "Mobile Responsive Behaviour" is complete — a working off-canvas mobile nav (hamburger → drawer, all real destinations, not just the desktop header's 4-5 links) now exists on both `templates/base.html` and `templates/adminhub/base.html`, following the existing Projects/Resume-modal JS convention exactly; a mobile-width audit pass found and fixed one real pre-existing bug (`.resume-entry.hub-card`'s desktop-only 250px right padding overflowing at mobile width). See `PHASES.md`, and Completed below.

Resolved 2026-08-29: [Phase 12] "Seeding Real Projects" is complete — a third real project, "Fire Service Website" (a real freelance client build for Iconic Techno Service), was seeded and made featured; both previously-open projects (TS Library, LoadGate) already had a real screenshot each, and the new one got one too, closing out the phase's "more real projects, and screenshots for both existing ones" open item. See `PHASES.md`, and Completed below.

Resolved 2026-08-28: [Phase 11] "Custom Admin Hub" is complete — a new `adminhub` app at `/adminhub/` visually clones Home/Projects/About/Contact/Hire Me/Blog/Resume with inline Edit/Delete/Add affordances over the seeded content, gated by a custom staff-only login. See `PHASES.md`, `DECISIONS.md`, `ARCHITECTURE.md`.

**[Phase 6] is superseded — merged into [Phase 8]** (owner's call, 2026-08-24), which is now complete. Timeline is cut from the roadmap entirely (not deferred); Contact gets no further scheduled work (considered already covered). See `PHASES.md`, `DECISIONS.md`.

Resolved 2026-08-24: the site brand mark badge swap is done — see Completed below.

Resolved 2026-08-25: Home's featured-project card and the Projects page are now driven by a real `Project` model ([Phase 8]) — no longer Phase 3's hardcoded mockup markup. Seeded rows are still placeholder-quality content by design; real project content is [Phase 12]'s job.

Resolved 2026-08-24: Designs, Blog, Resume, Timeline, and Hire Me all live in the `core` app; `projects` is scoped strictly to the owner's projects/works/code. **Designs still has no phase assigned yet** — when it's scheduled, it belongs in `core` per this decision. See `DECISIONS.md`.

Resolved 2026-08-26: [Phase 10]'s production database engine is PostgreSQL — connected and verified against both a local PostgreSQL install (day-to-day dev) and a Neon project (Phase 15's production target). Hosting (Render + an UptimeRobot keep-alive ping, interim until an international card enables a move to Oracle Cloud) moved to [Phase 15] rather than being part of Phase 10 (see `DECISIONS.md`).

## Blocked

None.

## Completed

- [x] [Phase 0a] Define feature set (Projects, Designs, Blog, Resume, About, Contact, Skills, GitHub links, Timeline, Hire Me)
- [x] [Phase 0a] Choose production stack: Django + Django templates + Tailwind CSS + React/JS
- [x] [Phase 0a] Define Django app boundaries: `core`, `contents`, `hiring` (superseded 2026-08-24 — see next entry)
- [x] [Phase 0a] Decide on global `templates/` and `static/` directories at project root (not per-app)
- [x] [Phase 0a] Confirm relational database as the persistence layer (Postgres vs MySQL deferred until a hosting decision) — 2026-08-24
- [x] [Phase 0b] Build static visual prototype (`prototype/index.html`) covering Home, Projects list/detail, About, and Contact pages
- [x] Initialize git repo, commit, and push to `origin` (`Port2309`, branch `main`) — 2026-08-24
- [x] Confirm prototype is a styling/design reference only; production build starts from scratch — 2026-08-24
- [x] Establish full build roadmap, Phase 0 (pre-development, done) through Phase 12 "Deployment and Hosting" — confirmed complete, no further phases expected — 2026-08-24 (see `PHASES.md`)
- [x] Simplify Django app boundaries to two apps (`core`, `projects`), superseding the earlier `core`/`contents`/`hiring` plan — 2026-08-24
- [x] [Phase 1] Initialize Django project from scratch (`manage.py` + `config/` package at repo root, `.venv`, `requirements.txt`) and verify `runserver` shows the default rocket page with no errors — 2026-08-24 (see `ARCHITECTURE.md`)
- [x] [Phase 2] Build global base template (`templates/base.html`) and Home page (`templates/pages/home.html`), wire up Tailwind CSS via npm + Tailwind CLI, matching `prototype/index.html`'s theme/structure with generic placeholder content — 2026-08-24 (see `ARCHITECTURE.md`, `DECISIONS.md`)
- [x] Add real hero portrait image to Home page (`static/images/hero-portrait.png`); added Pillow as a dependency for image processing (background removal) — 2026-08-24 (see `ARCHITECTURE.md`)
- [x] [Phase 3] Build About, Contact, and a static Projects list page (`templates/pages/{about,contact,projects}.html`), routed via `config/views.py`/`config/urls.py`; ported the matching CSS from `prototype/index.html` into `static_src/css/input.css`; wired nav/footer + Home's internal links to the real routes with active-page (`aria-current="page"`) marking — 2026-08-24 (see `ARCHITECTURE.md`, `PHASES.md`)
- [x] Add a placeholder GitHub profile page (`/github/`, `templates/pages/github.html`, new `.profile-card` CSS) — no live API fetch yet; trimmed LinkedIn/Email from the site footer (LinkedIn/Email stay plain outbound links elsewhere, no custom UI) — 2026-08-24, ad-hoc addition, not a numbered phase (see `DECISIONS.md`)
- [x] Clean up an AI-generated GitHub logo (background removed to transparent, watermark stripped, edges cleaned) and place it on the GitHub page's header (`static/images/github-logo.png`) — 2026-08-24, verified in-browser
- [x] [Phase 4] Scaffold `core`/`projects` apps via `manage.py startapp`; moved all views/routing out of `config/` into `core` (`core/views.py`, `core/urls.py` — now `ROOT_URLCONF`); `config/` reduced to `settings.py`/`wsgi.py`/`asgi.py` only; `projects` registered but left empty — 2026-08-24 (see `ARCHITECTURE.md`, `DECISIONS.md`, `PHASES.md`)
- [x] [Phase 5] Replaced placeholder content with the owner's real content site-wide (identity, Home hero/about-snapshot/statement/CTA, About page, Contact links) — 2026-08-24; explicitly excluded Home's featured-project card and the Projects page (owner's choice, stay on Phase 3 mockup content) and the site brand mark badge swap (deferred, owner wants to be asked first) — see `PHASES.md`, `DECISIONS.md`
- [x] [Phase 5] Wired `/github/` up to GitHub's public REST API for live avatar/name/bio/repo/follower/following data, with a static-real-values fallback on request failure; added `requests` to `requirements.txt` — 2026-08-24, verified in-browser (see `DECISIONS.md`, `ARCHITECTURE.md`) — this closes out the previously-open "wire the GitHub page up" item
- [x] Added a scrollable real-repository list to the GitHub page (`.repo-list`, beside the profile card), fetched live from GitHub's repos API, each entry linking out to the real repo — 2026-08-24, ad-hoc addition, verified in-browser (see `DECISIONS.md`, `ARCHITECTURE.md`)
- [x] Fixed the GitHub page's profile-card/repo-list layout to be equal width and height (switched `.github-layout` from flex to a stretch grid) — 2026-08-24, verified in-browser (see `DECISIONS.md`)
- [x] Made the GitHub logo in the page header a clickable button linking to the real GitHub profile, with a hover/scale affordance — 2026-08-24, verified in-browser
- [x] Cleaned the owner's real "SK" badge image (background removed via edge-based flood fill, watermark/noise dropped) and swapped it in as the site's nav/footer brand mark, replacing the auto-derived "S." text — 2026-08-24, verified in-browser (see `DECISIONS.md`, `ARCHITECTURE.md`) — this closes out the deferred badge-swap item
- [x] [Phase 7] Built placeholder `/blog/` page (three hardcoded posts, no model) and a styled `/resume/` page (real Skills section reusing About's data, placeholder Experience/Education, "Download PDF" wired to a pending static file); added a footer "Blog" link and a Home hero "See My Resume" button beside the portrait — 2026-08-25, verified in-browser (see `PHASES.md`, `DECISIONS.md`)
- [x] [Phase 8] Built a real `Project` model (`projects` app) with `project_list`/`project_detail` views, seeded 3 placeholder rows via a data migration; rewrote the Projects list page and Home's featured-project card to render from the database instead of hardcoded mockup markup; added a vanilla-JS/CSS project-detail modal on the Projects list page (with a real `/projects/<slug>/` page as the no-JS/shareable fallback); built "Hire Me" in `core` as a real backend-processed `/hire/` form (`HireRequest` model, `HireRequestForm`, admin-reviewable submissions, no email integration); added nav/footer "Hire Me" links — 2026-08-25, verified via direct HTTP checks (`manage.py check`/`migrate` clean, all routes 200, a real form submission posted via `curl` and confirmed saved, then removed as test data) — see `PHASES.md`, `DECISIONS.md`, `ARCHITECTURE.md`
- [x] Made the Contact page's form real/backend-processed too (`core.ContactRequest` model + `ContactRequestForm`, same POST/redirect/GET pattern as Hire Me, admin-reviewable) — post-Phase 8 ad-hoc addition, 2026-08-25, owner asked for this after seeing Hire Me; verified via `curl` (submission posted, confirmed saved, then removed as test data) — see `DECISIONS.md`, `ARCHITECTURE.md`
- [x] Created a Django superuser account (`Shahbaz`) for `/admin/` access — 2026-08-25
- [x] Made the Resume PDF admin-uploadable (`core.Resume` model with a `FileField`, `MEDIA_URL`/`MEDIA_ROOT` configured, dev-mode media serving in `core/urls.py`) — post-Phase 8 ad-hoc addition, 2026-08-25, resolves the Phase 7 "PDF deferred until dynamic" note; verified via a test upload (confirmed served at `/media/...`, then removed as test data) — see `DECISIONS.md`, `ARCHITECTURE.md`
- [x] Made the Home hero section admin-editable (`core.HeroContent` model — portrait, greeting, hero lines, roles, meta lines, year/location — seeded via data migration with the existing real content) — post-Phase 8 ad-hoc addition, 2026-08-25, owner asked to manage this from the database; verified `/` renders identically to the pre-change hardcoded version — see `DECISIONS.md`, `ARCHITECTURE.md`
- [x] Removed all remaining hardcoded hero text/image fallbacks from `home.html` and backfilled the real portrait image into `HeroContent` (`core/migrations/0006_backfill_hero_portrait.py`) — 2026-08-25, owner flagged the earlier `default:"..."` fallbacks as still having real content duplicated in code; verified `/` still renders correctly, fully DB-sourced (text + portrait) — see `DECISIONS.md`, `ARCHITECTURE.md`
- [x] Added Home's "Statement" line ("Good ideas deserve more than thought—they deserve to be built.") to `HeroContent` as a new `statement` field (not a new table, per owner's direction) and wired `home.html` to `{{ hero.statement }}` — 2026-08-25, verified via `curl` that the page renders the real text from the database — see `DECISIONS.md`, `ARCHITECTURE.md`
- [x] Split the quote into its own `HeroContent` row (added a `role` field — `primary`/`quote` — and a data migration creating the second row) after the owner asked for a second row in the admin list rather than a shared field; `core.views.home` now queries `role="primary"` and `role="quote"` separately, `home.html` reads `{{ quote.statement }}` — 2026-08-25, verified via a live admin login (list shows 2 rows with a Role column) and `curl` on `/` (both the identity content and the quote still render correctly) — see `DECISIONS.md`, `ARCHITECTURE.md`
- [x] Hid the `statement` field from the primary row's admin change form (`HeroContentAdmin.get_exclude`, only shown when editing the `quote`-role row) — 2026-08-25, owner asked for it removed from `/admin/core/herocontent/1/change/` since it's unused there; verified via a live admin request that row 1's form omits it while row 2's still has it
- [x] Split the quote out of `HeroContent` into a new, dedicated `core.Quote` model — removed `role`/`statement` (and the `ROLE_*` constants) from `HeroContent`, added `Quote` (`statement`, `updated_at`) with its own `QuoteAdmin`; `core.views.home` now queries `HeroContent.objects.first()`/`Quote.objects.first()` independently — 2026-08-25, owner found the `role`-discriminated single-table approach still too complex for a single string; verified via `manage.py makemigrations --check` (clean), `migrate`, a shell check (1 `HeroContent` row, 1 `Quote` row, correct data), and a live `runserver` request confirming `/` renders identically (hero + quote text) and both `/admin/core/herocontent/` and `/admin/core/quote/` resolve — see `DECISIONS.md`, `ARCHITECTURE.md`
- [x] Made Home's "A LITTLE ABOUT ME" snapshot section model-backed (`core.AboutSnapshot` — eyebrow, 3 headline words, headline sub-line, paragraph, 4 "Currently" lines), admin-registered, seeded with the section's existing real content (`0013_aboutsnapshot.py`/`0014_seed_about_snapshot.py`); `core.views.home` and `home.html` updated to source the section from `about_snapshot` — 2026-08-25, owner asked for the same `HeroContent`-style treatment for this section; verified via `makemigrations --check` (clean), `migrate`, and a live `runserver` request confirming `/` renders the section's real text from the database and `/admin/core/aboutsnapshot/` resolves — see `DECISIONS.md`, `ARCHITECTURE.md`
- [x] Removed the real-content `default=...` values from every field on `HeroContent`, `Quote`, and `AboutSnapshot` in `core/models.py` — owner flagged the model file as still carrying real text in code, duplicating the database rows; applied as `core/migrations/0015_remove_content_defaults.py` (`AlterField`-only, no data touched); verified `manage.py check` clean and `/` still renders identically from the already-seeded rows — see `DECISIONS.md`
- [x] Made the contact email model-backed (`core.ContactEmail`), removed every hardcoded `shahbazkhan74659@gmail.com` occurrence from `home.html`/`contact.html`/`hire.html`, replaced with `{{ contact_email.email }}` — wired site-wide via a new context processor (`core/context_processors.py`, registered in `config/settings.py`) rather than per-view queries, since three separate views needed it — 2026-08-25, owner asked for a dedicated table plus removal of every hardcoded copy; verified via `makemigrations --check` (clean), `migrate`, a repo-wide grep confirming the address only remains in the seed migration (`0017_seed_contact_email.py`) and the untouched `prototype/index.html` (fictional placeholder, not the real address), and live `runserver` requests to `/`, `/contact/`, `/hire/`, and `/admin/core/contactemail/` — see `DECISIONS.md`, `ARCHITECTURE.md`
- [x] Made the whole About page model-backed (`core.About` — page head, Background/What-I-care-about, Philosophy, Skills & Tools with 3 tag groups reusing `Project.tag_list()`'s pattern, Where this is going), seeded with the page's existing real content (`0018_about.py`/`0019_seed_about.py`), every hardcoded string removed from `about.html` and replaced with `{{ about.* }}` — 2026-08-25, owner asked for a dedicated table for the whole page including structural headings as fields; verified via `makemigrations --check` (clean), `migrate`, a live `runserver` request to `/about/` confirming all sections/paragraphs/quote/12 skill tags render correctly, a repo-wide grep confirming the page's real strings only remain in the seed migration, and `/admin/core/about/` resolving — see `DECISIONS.md`, `ARCHITECTURE.md`
- [x] Added `github_display`/`linkedin_display` to `core.ContactEmail`, seeded with the real display strings (`0020_contactemail_links.py`/`0021_seed_contact_links.py`); wired onto `contact.html`'s existing GitHub/LinkedIn rows (display text only — hrefs left unchanged) and added matching new rows to `hire.html` (which had none) — 2026-08-25, owner clarified only the display "names" should move to the database, not the linking mechanism; verified via `makemigrations --check` (clean), `migrate`, a shell check of the seeded row, live `runserver` requests to `/contact/` and `/hire/` confirming correct display text and unchanged hrefs (`/github/` internal route, external LinkedIn URL), and `/admin/core/contactemail/` resolving — see `DECISIONS.md`, `ARCHITECTURE.md`
- [x] Added `available_for_label`/`available_for` and `stack_label`/`stack` to `core.ContactEmail`, seeded with Hire Me's real "Available for"/"Stack" row content (`0022_contactemail_hire_fields.py`/`0023_seed_contact_hire_fields.py`); `hire.html`'s two rows now render from the database, no hardcoded label or value remains — 2026-08-25, owner asked for the same table/pattern as the GitHub/LinkedIn addition; verified via `makemigrations --check` (clean), `migrate`, a live `runserver` request to `/hire/` confirming both rows render correctly, a grep confirming no hardcoded copy remains, and `/admin/core/contactemail/` resolving — see `DECISIONS.md`, `ARCHITECTURE.md`
- [x] [Phase 10] Connected the site to PostgreSQL — `config/settings.py` reads `DATABASE_URL` and configures Postgres via the new `psycopg` dependency, falling back to SQLite when unset; created a second Neon project (separate from the owner's existing one) for this portfolio and ran `manage.py migrate` clean against it (all existing migrations, including seed data), with a Neon superuser (`Shahbaz`) created — 2026-08-26, verified via `runserver` serving `/` and `/projects/` with real/seeded content pulled from Neon, and `/admin/` returning its normal login redirect
- [x] [Post-Phase 10] Switched local day-to-day development from SQLite to a local PostgreSQL 18 install already running on this machine — created a dedicated `portfolio_user` role and `portfolio` database, ran `manage.py migrate` clean against it, created a matching local superuser (`Shahbaz`), and pointed the local `.env`'s `DATABASE_URL` at it (Neon's connection string kept commented out in the same file for later); Render deployment + the UptimeRobot keep-alive ping moved to [Phase 15] instead of staying in Phase 10 — 2026-08-26, verified via a restarted `runserver` serving `/` and `/projects/` (3 seeded entries) from the local database — see `PHASES.md`, `DECISIONS.md`, `ARCHITECTURE.md`
- [x] [Phase 9] Finished and polished the frontend and backend across 8 owner-confirmed items: accessibility fixes (descriptive hero portrait alt text, `.contact-row-link` CSS class replacing inline styles), a responsive/mobile fix for the GitHub page's fixed-height `.profile-card`/`.repo-list` panels, visible accessible form validation errors on Contact/Hire Me, site-wide `scroll-behavior: smooth`, custom `404`/`500`/`403` error templates, environment-variable-driven `SECRET_KEY`/`DEBUG`/`ALLOWED_HOSTS` (via `python-dotenv`, dev-safe defaults, new `.env.example`), 15-minute cached GitHub API calls (Django's default `LocMemCache`), and a shared `_handle_lead_form` helper de-duplicating the Contact/Hire Me views — 2026-08-25, scope confirmed via AskUserQuestion from a codebase-grounded candidate survey (SEO/meta items surveyed but explicitly excluded); verified via `manage.py check` (clean), invalid/valid form submissions via direct POST, a monkeypatched-`requests.get` proof that the GitHub view's second call makes zero live API requests within the cache TTL, a live styled-404 check under a temporary real `DEBUG=False`/`ALLOWED_HOSTS` `.env` (then reverted), and dry-run template rendering of all three error pages — see `PHASES.md`, `DECISIONS.md`, `ARCHITECTURE.md`
- [x] [Phase - X, partial] Built the real Blog model/logic/tables layer: `projects.Post` (title, slug, short_description, content, tags, published_at) and a shared `projects.Tag` model, placed in `projects` rather than `core` (deliberate override of the standing Designs/Blog/Resume/Timeline/Hire-Me → `core` placement decision, Blog only — see `DECISIONS.md`); `blog_list`/`blog_detail` views + `projects/blog_urls.py` (`include()`d from `core.urls` at `/blog/`, `blog` url name preserved, `blog_detail` new); `blog.html` converted from 3 hardcoded entries to a `{% for post in posts %}` loop, new `blog_detail.html` page; 1 placeholder `Post` + 3 placeholder `Tag` rows seeded via a data migration. The new `Post.tags` field enforces a 1–6 count via a custom `PostAdminForm.clean_tags()`. Per an owner follow-up ("use Real Tag model for all the tables where tags used"), also converted `projects.Project.tags` and `core.About.languages`/`.frameworks`/`.learning` from comma-separated `CharField`s to the same shared `Tag` M2M (via safe multi-step migrations preserving existing data) — `resume.html`'s hardcoded Skills markup was identified and explicitly excluded (no backing table). 2026-08-26; verified via `manage.py check` (clean), `makemigrations --check` (clean against final model state), a Django shell check confirming `About`'s real skill words survived the conversion intact, a `Client()`-driven admin POST test confirming 0/7 tags are rejected and 1–6 succeed, and `runserver` checks on `/blog/`, `/blog/post-title-one/`, `/projects/`, `/about/`, and `/` (footer link, featured-project tags, and About's skill tags all still render correctly) — see `PHASES.md`, `DECISIONS.md`, `ARCHITECTURE.md`
- [x] [Phase - X, complete] Wrote and seeded the first real blog post — "Building This Portfolio" (slug `building-this-portfolio`), the real story of this site's own build (prototype → Django rebuild → app boundaries → the "don't fabricate content" rule → content going database-backed → the Postgres move → this post itself as the next step in that same pattern). Data migration `projects/migrations/0009_seed_real_first_post.py` deleted the placeholder `Post` (`post-title-one`) and its now-orphaned placeholder `Tag` rows (`Tag One`/`Two`/`Three`), then seeded the real post tagged with 4 existing real `Tag` rows (`Django`, `Python`, `Tailwind CSS`, `Relational DB`) — no new tags invented. 2026-08-26; verified via `manage.py migrate` (clean), a shell check confirming the placeholder post/tags are gone and the real post/tags are present, and `runserver` requests confirming `/blog/` and `/blog/building-this-portfolio/` return 200 while `/blog/post-title-one/` now 404s — see `PHASES.md`.
- [x] [Phase 12, partial, ad hoc] Seeded the first real `Project` — "TS Library," a Spring Boot/Thymeleaf/MySQL library management system, deployed live on Render with an Aiven-hosted MySQL database. Data migration `projects/migrations/0011_seed_real_first_project.py` deleted all 3 Phase 3/5 placeholder `Project` rows (owner's explicit choice, asked directly since it wasn't a clean 1:1 swap like the blog case) and seeded the real row — title, both short and long-form descriptions (sourced verbatim from `project.txt`, only ASCII file-formatting artifacts stripped), category, role, year, status (trimmed to fit the 30-char field limit), GitHub/Live URLs, and 5 new `Tag` rows (Java, Spring Boot, Thymeleaf, MySQL, Docker), flagged `featured=True`. No screenshots yet — `project.txt` had none, so it renders via the `.mockup` fallback Phase Y built until real `ProjectImage` rows are uploaded via admin. 2026-08-27; verified via `manage.py migrate`/`makemigrations --check` (clean), `runserver` requests confirming `/`, `/projects/`, `/projects/ts-library/` render correctly (exactly 1 project, correct tags/category/year/role/status, both GitHub and Live buttons present, all 9 description sections intact) and the 3 old placeholder detail URLs now 404, and a shell check confirming the now-orphaned placeholder `Tag` ("Tag") was cleaned up — see `PHASES.md`, `DECISIONS.md`.
- [x] [Phase Y, complete] Upgraded the Projects page's logic and interface: added `projects.ProjectImage` (gallery model, FK'd to `Project`, powers both the list-card's primary image and the detail page's full gallery, falling back to the existing `.mockup` chip when a project has no images) and `Project.description` (long-form detail-page body); added category/tag filtering, free-text search, and pagination to `/projects/` (GET-param driven, AND-combinable, bookmarkable, via Django's `Paginator`); gave `Project.tags` the same 1–6 admin validation `Post.tags` already had; removed Home's hardcoded featured-project placeholder fallback (the `{% else %}` branch), following the no-template-fallback convention already established by `HeroContent`/`Quote`/`AboutSnapshot`. 2026-08-27; verified via `manage.py check`/`makemigrations --check` (clean), `runserver` requests across every filter/search/pagination combination (including an out-of-range page clamping instead of erroring) and the empty-result state, a real test image uploaded via the ORM and confirmed rendering on the list card/modal/detail gallery/Home's featured card then removed (mockup fallback confirmed returning), `ProjectAdminForm.clean_tags()` exercised directly (0 and 7 tags both rejected), and the admin inline's formset fields confirmed present via an authenticated test-client request — see `PHASES.md`, `ARCHITECTURE.md`, `DECISIONS.md`.
- [x] [Phase 12, in progress] Uploaded LoadGate's first real screenshot — `projects.ProjectImage` (id 3, `order=0`, caption "Truck loading entries dashboard") created via the ORM (not a migration — matches the established "admin-uploaded, not seeded" convention for this model), replacing the `.mockup` fallback with a real dashboard screenshot of the live client app (truck-entries table, S.No/date/vehicle/weight columns). The source screenshot was checked first: its vehicle numbers (e.g. `MH66HLW6527`) were confirmed to match `seed_entries`' random-generator format exactly (state + RTO + letters + digits) — synthetic dev-seed data, not real client records — and the header read "Welcome, Admin" (this session's own just-reset dev credentials), so nothing real/identifying is in the image. 2026-08-28; verified via `runserver` requests confirming the image renders on `/`'s featured card, both cards on `/projects/`, and `/projects/loadgate/`'s gallery, and that `/media/projects/loadgate-dashboard.png` serves 200 — see `DECISIONS.md`.
- [x] [Phase 12, in progress] Renamed the "Entry Recorder" `Project` row to **LoadGate** (owner's chosen product name — matches the name the client project's own Render Blueprint was already deployed under, `LoadGate_Entries`) — `title`→`"LoadGate"`, `slug` `entry-recorder`→`loadgate`, and both in-prose mentions of the old name inside `description` updated to match (`projects/migrations/0014_entry_recorder_rename_loadgate.py`, reversible). 2026-08-28; verified via `manage.py check`/`makemigrations --check` (clean), `migrate`, a shell check confirming the new title/slug and an intact em-dash in `description`, and `runserver` requests confirming `/projects/loadgate/` now 200s, the old `/projects/entry-recorder/` now 404s, and "LoadGate" renders on `/` and `/projects/` — see `DECISIONS.md`.
- [x] [Phase 12, in progress] Removed LoadGate's `live_url` (`projects/migrations/0013_entry_recorder_remove_live_url.py`, reversible) — the app is single-user/login-gated, so a public "Live" button led visitors to a login wall they can't get past; the GitHub link stays. No template change needed, since the Live-URL button was already independently `{% if %}`-guarded (Phase Y). 2026-08-28; verified via `manage.py check`/`makemigrations --check` (clean), `migrate`, a shell check confirming `live_url` is now blank while `github_url` is untouched, and `runserver` requests confirming no `entryrecorder.onrender.com` reference remains on `/`, `/projects/`, or the project's detail page — see `DECISIONS.md`.
- [x] [Phase 12, in progress] Seeded a second real `Project`, initially titled "Entry Recorder" and same-day renamed to **LoadGate** (see the rename entry above) — a single-user Django/PostgreSQL web app built for a manufacturing client, replacing a paper truck-loading logbook (entry CRUD, batching, PDF export, account security). Its GitHub repo (`RecordEntryApp`) was private; confirmed clean of committed secrets (`.env` never tracked, `SECRET_KEY` read from environment, only test-fixture passwords in code) and switched to public so the portfolio's GitHub button resolves. Seeded via `projects/migrations/0012_seed_second_real_project.py` with 4 tags (Django, Python, PostgreSQL, JavaScript — reusing existing `Tag` rows where they already existed). Made `featured=True` (owner's choice) — TS Library's row updated to `featured=False`/`order=2` in the same migration. Client identity kept generic in the description (owner's choice) — described as "a manufacturing client," not named. No screenshots seeded yet (same `.mockup` fallback as TS Library). 2026-08-28; verified via `manage.py check`/`makemigrations --check` (clean), `migrate`, a shell check confirming order/featured/tags on both `Project` rows, and `runserver` requests confirming `/`, `/projects/`, the project detail page, and `/projects/ts-library/` all 200 with correct featured card, GitHub/Live links, and tag chips — see `PHASES.md`, `DECISIONS.md`.
- [x] [Phase 12, complete] Seeded a third real `Project`, **Fire Service Website** — a production Django 5.2 + React 19/TypeScript + PostgreSQL business site with a custom staff-only Admin Hub CMS and three React-island lead-capture forms (Survey, Contact, Consultation), built as a real freelance engagement for **Iconic Techno Service**, a fire & life-safety systems company. Client identity named in the description (owner's explicit choice this time, unlike LoadGate's generic "a manufacturing client" framing). Seeded via `projects/migrations/0015_seed_third_real_project.py` with 5 reused `Tag` rows (Django, React, TypeScript, PostgreSQL, REST APIs — no duplicates created) and made `featured=True` (owner's choice) — LoadGate's row updated to `featured=False`/`order=2`, TS Library shifted to `order=3`, in the same migration. Deployed live on Render (`https://iconic-techno-service.onrender.com`, linked as `live_url`) backed by Neon PostgreSQL; per the owner, currently being migrated to a self-hosted Oracle Cloud "Always Free" VM to eliminate Render free-tier cold-start delays (noted in the description's "Current Status" section). An owner-supplied real screenshot of the live Home page was uploaded as its first `ProjectImage` via the ORM (admin-uploaded convention, not a migration), same pattern as LoadGate's screenshot. 2026-08-29; verified via `manage.py check`/`makemigrations --check` (clean), `migrate` (applied cleanly), a shell check confirming all three `Project` rows' `order`/`featured`/`tags` and that every project now has at least one `ProjectImage`, and `runserver`+in-browser checks confirming `/`, `/projects/`, and `/projects/fire-service-website/` render correctly with the real screenshot, tags, and GitHub/Live links — see `PHASES.md`.
- [x] [Ad hoc, post-Phase 12] Fixed the Resume page's "DOWNLOAD PDF" button (it only opened the PDF in a viewer tab, never actually downloaded it) and added a new "VIEW RESUME" button to its left that opens the PDF in an in-page popup modal — on both `templates/pages/resume.html` and `templates/adminhub/resume.html`. Download now uses the HTML5 `download="Shahbaz-Khan-Resume.pdf"` attribute instead of `target="_blank"` (same-origin `/media/resume/...` file, no CDN in this project, so it reliably forces a real save rather than a navigation). The View modal reuses the Projects page's existing popup-modal pattern (backdrop/panel/close button, `data-modal-trigger`/`data-modal-target`, Escape/backdrop-click-to-close) adapted for an `<iframe>` PDF preview instead of cloned HTML — new `.resume-modal-backdrop`/`.resume-modal-panel`/`.resume-modal-iframe`/`.resume-actions` CSS added to `static_src/css/input.css` (kept separate from `.project-modal-*` rather than reusing them, since an iframe needs an explicit `height` and no panel padding, unlike the text-content Projects modal). **Also required a settings fix to actually work**: Django's default `X_FRAME_OPTIONS = 'DENY'` (never previously overridden in this project) blocks any page — including the site's own — from framing its own content in an `<iframe>`, which silently broke the PDF preview (blank/broken-image iframe) until `config/settings.py` was given an explicit `X_FRAME_OPTIONS = 'SAMEORIGIN'` (real cross-site clickjacking protection is unchanged — only same-origin framing, which this feature needs, is now allowed). 2026-08-29; verified via direct template rendering (`get_template().render()`, no errors) and Django test-client requests for the Admin Hub page (staff-authenticated, confirmed 200 with correct button/modal counts), and live in-browser (Chrome extension) on the public page — clicking "VIEW RESUME" opens the modal with the real PDF rendering fully inside via Chrome's native PDF viewer (page thumbnails, zoom, toolbar all functional), and the close button correctly hides the modal and clears the iframe `src`.
- [x] [Ad hoc, post-Phase 12] Made every external link across the public site and Admin Hub open in a new tab (`target="_blank" rel="noopener"`) instead of navigating away from the portfolio — GitHub/Live-URL project links (`projects.html`'s list cards, modal template, and detail page), the GitHub page's profile link, "View on GitHub" button, and every repo-list item, the Contact/Hire Me/Home pages' `mailto:` and LinkedIn links, and the Admin Hub's Contact/Hire Me submission `mailto:` links. Internal routes (the portfolio's own `/github/` page, footer/nav links, `{% url %}` tags) were deliberately left as normal same-tab navigation — only links leaving the site got the change. Matches the `rel="noopener"` convention the Resume page's "Download PDF" button already used. 2026-08-29; verified in-browser (Chrome extension) on `/contact/` — clicking LinkedIn opened a separate tab while the original page stayed put — and via a repo-wide grep confirming every external `href` (`mailto:`, `https://`, `project.github_url`/`live_url`, GitHub API `html_url`) now carries `target="_blank" rel="noopener"`.
- [x] [Ad hoc, post-Phase 11] Fixed the neon-green "SEE MY RESUME" button's glow/border rendering as a giant page-spanning ellipse on `/projects/` — the shared `partials/resume_button.html` button's `.btn-neon::before`/`::after` pseudo-elements (`position: absolute`) had no positioning context of their own; on Home it worked by accident because the page-specific `.hero-resume-btn` modifier happened to set `position: absolute`, but on Projects (no such modifier) the pseudo-elements fell back to the viewport-sized initial containing block. Fixed by giving `.btn-neon` its own `position: relative` in `static_src/css/input.css` (reordering `.hero-resume-btn` to stay after it in the cascade so Home's absolute-positioning override still wins), and added a `translateY(-3px)` hover-lift directly to `.btn-neon` so the button lifts on hover consistently on every page, not just Home. Then rolled the same shared resume button out to `/about/`, `/contact/`, and `/hire/` (previously Home/Projects only), each wrapped in the same `page-head-row` layout with a fixed `margin-top:50px` offset so the button lands in the exact same on-screen position across all four pages — deliberate, so it doesn't visibly jump when navigating between them. 2026-08-29; verified in-browser (Chrome extension) on all four pages at desktop width, confirming the border/glow stays scoped to the button only, the hover lift works everywhere, and Home's pre-existing hover behavior is unchanged. Not part of Phase 12's scope (no project data changed) — logged here as ad hoc UI work, same pattern as prior unphased fixes.
- [x] [Ad hoc, post-Phase 11] Replaced the nav's "SHAHBAZ KHAN" text logo with the SK badge image — moved `.nav-mark`/`.nav-mark-badge` (the badge previously sitting at the far right end of the nav links) to the left in place of the `.nav-name` text link, and removed the "SHAHBAZ KHAN" text entirely, in both `templates/base.html` (public site) and `templates/adminhub/base.html` (admin hub) — footer's separate "© {year} Shahbaz Khan" text and its own badge were left untouched (out of scope). 2026-08-29; verified in-browser on both the public nav and the admin hub nav (including the "Log out" button still rendering correctly alongside it).
- [x] [Phase 11, complete] Built the Custom Admin Hub: a new `adminhub` app at `/adminhub/` (own `templates/adminhub/` directory), gated by a custom staff-only login (`/adminhub/login/`, Django's existing session auth, `/admin/` untouched) — visually cloning Home/Projects/About/Contact/Hire Me/Blog/Resume with small inline Edit/Delete affordances (and Add for Project/Post/Resume-Experience/Resume-Education) over the seeded content; Contact/Hire Me show a placeholder notice card in place of the public forms plus a "Recent Submissions" list (delete-only) for `ContactRequest`/`HireRequest`. Resume was upgraded from "only its PDF file is model-backed" to fully editable: three new `core` models (`ResumePage`, `ResumeExperience`, `ResumeEducation`) replaced its hardcoded Skills/Experience/Education markup, seeded via `core/migrations/0027`/`0028` with the exact pre-existing placeholder content — `templates/pages/resume.html` now renders from the database. Tags (Project/Post/About/ResumePage) are managed via an in-form checkbox picker plus a same-page "quick add" box that creates a new `Tag` without losing other field values or needing any new JS; the existing 1–6 tag-count rule was duplicated into the hub's forms and extended to `About`/`ResumePage`'s tag fields. 2026-08-28; verified via `manage.py check`/`makemigrations --check` (clean), full CRUD cycles through Django's test client (Project incl. image formset and tag quick-add, Post, singleton edits/deletes, Contact/Hire submission review-and-delete matching `/admin/`'s same table), tag-count validation matching existing admin behavior, and a live in-browser walkthrough of all 7 hub pages after logging in — see `PHASES.md`, `DECISIONS.md`, `ARCHITECTURE.md`.
- [x] [Phase 13, complete] Built a real off-canvas mobile nav — a hamburger button (`.nav-burger`) swaps in for the desktop `.nav-links` row below the existing `640px` breakpoint, opening a right-side slide-in drawer (`.nav-drawer`/`.nav-drawer-backdrop`) listing every real page (Home, Projects, About, Blog, Resume, Contact, Hire Me, GitHub on the public site; the 7 Admin Hub pages plus a re-skinned "Log out" row on the hub) — not just the 4-5 links the desktop header shows. Implemented in both `templates/base.html` and `templates/adminhub/base.html` as an always-present `<script>` (deliberately kept outside `{% block extra_body %}`, which `projects.html`/`resume.html`/`adminhub/resume.html` already override rather than extend), following the exact IIFE/`hidden`-toggling/Escape-key/backdrop-click/`panel.focus()` convention the existing Projects-detail and Resume-preview modals already use. Preceded by a static design-canvas prototype (mobile mockups of all 8 public pages in the site's real theme/tokens, reviewed with the owner) that this phase implements for real. A mobile-width audit pass (a same-origin iframe was used as the practical narrow-viewport substitute, since `resize_window` didn't affect the actual browser viewport in this session) found and fixed one real pre-existing bug: `.resume-entry.hub-card`'s desktop-only `padding-right: 250px` (reserved for the absolutely-positioned Edit/Delete buttons) squeezed Admin Hub Resume entries into a ~100px content column at mobile widths. 2026-09-03; verified via `manage.py check` (clean), `npm run build:css` (clean), a live narrow-viewport walkthrough of Home (drawer open/close via burger/link-click/close-button, correct `aria-current` accent-dot) and Projects (existing filter bar, tag chips, and the pre-existing project-detail modal all confirmed unaffected by the new script), and a direct authenticated template render confirming the Admin Hub drawer's markup and all 7 hub URL names resolve with no `NoReverseMatch`. A full interactive click-through of the Admin Hub's own form pages at mobile width was intentionally left undone in-session (avoided the login flow after the browser autofilled a saved password into it) — see `PHASES.md`.
- [x] [Ad hoc, post-Phase 13] Fixed a second instance of the "SEE MY RESUME" button's glow/border escaping its own bounds into a giant shape — this time on Home specifically at mobile width, owner-reported via screenshot. Root cause: the `900px` breakpoint's `.hero-resume-btn{ position: static; ... }` (pre-existing, not introduced by Phase 13) reset `position` away from `.btn-neon`'s base `position: relative`, so its `::before`/`::after` glow pseudo-elements lost their own containing block and escaped up to `.hero` (`position: relative`, spanning the whole hero section) — the same root cause as the earlier post-Phase-11 fix of this button on `/projects/`, just re-triggered by this mobile rule instead. Changing `position` to `relative` alone caused a second bug (button shot ~570px off-screen), because unlike `static`, `relative` honors the desktop rule's `top`/`right` offsets (originally used to park the button beside the portrait) — fixed by also setting `top: auto; right: auto` in the same mobile rule, so the button stays centered under the portrait exactly as before while its pseudo-elements now stay correctly scoped. 2026-09-03; verified via a live narrow-viewport (390px) check: the button's bounding rect confirmed on-screen and correctly sized/positioned, and visually confirmed matching the intended tight-pill glow (owner-supplied reference screenshot) with no page-spanning artifact.
- [x] [Phase 15, partial] Made the codebase deploy-ready for Render: added `gunicorn` (WSGI server), `whitenoise` (production static-file serving, `MIDDLEWARE` + DEBUG-gated `STORAGES['staticfiles']`), production-only security settings (`CSRF_TRUSTED_ORIGINS`, `SECURE_PROXY_SSL_HEADER`, cookie-secure flags, `SECURE_SSL_REDIRECT`/HSTS), `.python-version` (3.11.9), and a `render.yaml` Blueprint defining the Render web service end-to-end. Media storage went through two providers: Cloudflare R2 (`django-storages`+`boto3`) was tried first but turned out to require a card on file to activate at all (confirmed live in Cloudflare's dashboard) — switched same-day to **Cloudinary** (`cloudinary`+`django-cloudinary-storage`, no card required), with `STORAGES['default']` branching on `CLOUDINARY_CLOUD_NAME` the same way `DATABASE_URL` already branches for Postgres. A dedicated `portfolio-render` Cloudinary API key was created (Master Admin role, email-verified) rather than reusing the account's existing Root key. Discovered and fixed a real bug during verification: Cloudinary's default storage forces `resource_type='image'`, which rejects the Resume PDF — added a `resume_storage()` callable (`core/models.py`) routing `Resume.file` to `RawMediaCloudinaryStorage` instead, with a new migration (`core/migrations/0031_alter_resume_file.py`). 2026-09-03; verified via `manage.py check` (clean), a simulated production `manage.py check --deploy` (clean bar the pre-existing intentional `X_FRAME_OPTIONS` warning), a real `collectstatic` run (131 files, 387 post-processed, no errors), and a real upload/delete round-trip through both Cloudinary storage classes (an actual PNG and a text file), confirmed removed via Cloudinary's Admin API directly (not just Django's `.exists()`, which this package doesn't implement meaningfully). Account-level steps (Render service creation, first deploy, UptimeRobot) are not yet done — see the Next section above and `DECISIONS.md`, `ARCHITECTURE.md`, `PHASES.md`.
- [x] [Phase 15, complete except UptimeRobot] Created the Render web service (`shahbazkhan`, named for the owner per the standard firstlast portfolio-domain convention) from the repo's `render.yaml` Blueprint and got it fully live at `https://shahbazkhan.onrender.com`. The first deploy attempt failed at the build step; found and fixed three real, sequential bugs (each verified locally, pushed, and re-deployed): `django-cloudinary-storage`'s `collectstatic` override crashing on the legacy `STATICFILES_STORAGE` attribute (fixed by also setting it for back-compat); the same override still winning and only partially collecting Django admin's static files, since Django resolves same-named commands by first-`INSTALLED_APPS`-entry-wins (fixed by reordering so `django.contrib.staticfiles` comes first, bypassing the override entirely — confirmed by reading Django's own command-resolution source, not assumed); and `render.yaml`'s build command never running `npm run build:css`, so the gitignored Tailwind stylesheet never existed on Render (fixed by prepending the npm build step). Also found and fixed a real data gap, not a code bug: Neon had zero `Resume`/`ProjectImage` rows, since those were only ever uploaded via the ORM against the *local* Postgres dev database (post-Phase-10 convention), never against Neon — fixed with a one-time, uncommitted script that uploaded the real local files to Cloudinary and created the matching rows directly against Neon. 2026-09-03; verified in-browser on the live site: correct Tailwind styling, real hero portrait/nav badge/all three project screenshots loading from Cloudinary, `/projects/` showing all 3 real projects with working filters. See `DECISIONS.md`'s "Render service named `shahbazkhan`; first live deploy" entry for full detail. Only the UptimeRobot monitor is left — see the Next section above.
- [x] [Phase 15, complete] Set the production superuser (`Shahbaz`, created in Phase 10) to the owner's chosen production password directly against Neon — verified via a direct `authenticate()` check, not just a save. Created a free UptimeRobot monitor for `shahbazkhan.onrender.com` (HTTP, 5-minute interval, e-mail alerts to the owner's address) — confirmed "Up", 100% uptime shortly after creation. 2026-09-03; this closes out every completion criterion for [Phase 15] — the site is deployed, reachable, connected to Neon, and actively monitored to prevent Render's free-tier idle spin-down. [Phase 15] and the whole numbered roadmap are now complete.
- [x] [Phase 16, first slice] Converted the Projects page (`/projects/`) filter/search/pagination to AJAX — search box (debounced live search), tag chips, category dropdown, pagination, and "Clear filters" all update `#proj-results` via `fetch()` against the existing `project_list` view, with zero full-page reloads and zero browser-URL changes (no `history.pushState`/`replaceState`, ever), per the owner's explicit requirement. Implemented via header-based content negotiation (`X-Requested-With: XMLHttpRequest` → a new `partials/project_results.html` fragment, else the full page — identical context both times), `AbortController`-based race safety for fast typing, and event delegation on `#proj-results` for the modal-open triggers and pagination links specifically (the only two elements living inside the AJAX-swapped region). 2026-09-03; verified via `manage.py check` (clean), direct curl/header-based checks confirming the partial-vs-full-page branch, tag/search/category filtering, and the empty-result state, plus a no-JS/direct-link fallback check (`?q=...&category=...&tag=...&page=...` still server-renders correctly) — see `PHASES.md`, `DECISIONS.md`, `ARCHITECTURE.md`.
- [x] [Phase 16, follow-up fix] Fixed the "Clear filters" link disappearing permanently after the AJAX conversion above — it was only rendered into the DOM when a filter was already active on the initial page load, so on a normal unfiltered visit the JS could never find it later no matter what got filtered (the filter bar sits outside the AJAX-swapped region and is never re-rendered by the server after first load). Fixed by always rendering the link and toggling it with the `hidden` attribute instead of `{% if %}`-omitting it. 2026-09-03; verified via curl confirming the link is always present with `hidden` correctly toggled based on filter state — see `DECISIONS.md`.
- [x] [Phase 16, second slice, complete] Converted the Contact and Hire Me pages' forms to AJAX submission — clicking "Send message"/"Send request" no longer does a full page reload (and, since the AJAX path never calls `redirect()`, no `?sent=1` URL change either). Extended the shared `core._handle_lead_form` helper with the same header-based negotiation as the Projects page; new `partials/contact_form.html`/`hire_form.html` render either the success message or the form re-shown with errors (always HTTP 200); each page's `submit` listener is delegated on the persistent `<div id="...-form-wrap">` rather than the `<form>` itself, since the form gets replaced on every response. A network-failure handler preserves the user's typed input rather than wiping the form. 2026-09-03; verified via direct AJAX POST for both pages: an invalid submission returns the same field-level `aria-invalid`/`role="alert"` errors as before; a valid submission returns the success partial and a real `ContactRequest`/`HireRequest` row confirmed saved then removed as test data; a plain POST without the AJAX header still 302-redirects to `?sent=1` exactly as before, confirming the no-JS/PRG fallback is unaffected — see `PHASES.md`, `DECISIONS.md`, `ARCHITECTURE.md`. **[Phase 16] and the whole roadmap are now complete**, at the owner's explicit call to close the phase at these two delivered slices.
- [x] [Phase 17, complete and deployed] Built out SEO Optimization: a favicon (owner-supplied `skbadge.png`, used as-is, no processing, copied to `static/images/favicon.png`); `core.views.robots_txt` (`/robots.txt`, disallows `/admin/`/`/adminhub/`, dynamically points at the live sitemap); `core/sitemaps.py` (`django.contrib.sitemaps`, no `django.contrib.sites`/`SITE_ID` needed) serving `/sitemap.xml` with every static route plus all real `Project`/`Post` rows; a new `core.context_processors.canonical_url` context processor so every page — including Projects' filtered/paginated query-param URLs — carries a correct canonical `<link>` pointing at its clean path; Open Graph + Twitter Card tags in `base.html` (Twitter relies on `og:*` fallback rather than duplicating tags), with `project_detail.html`/`blog_detail.html` overriding `og_title`/`og_description`/`og_image` with the real project/post's own data; a site-wide JSON-LD `Person` schema (`templates/partials/schema_person.html`) naming all six target locations (Silvassa, Vapi, Daman, DNH, Bhilad, Naroli) as `workLocation` — the deliberately non-spammy alternative to the keyword-stuffed titles found on real local competitor sites surveyed during planning; and real, owner-approved local-SEO copy added via `core/migrations/0032_local_seo_copy.py` (a new closing sentence on `About.background_paragraph`, a location suffix on `ContactEmail.available_for`) plus matching tweaks to About/Hire Me's meta descriptions. One real implementation-time discovery: Django rejects a `{% block %}` name used twice in the same template (a `TemplateSyntaxError`, hit and fixed live) — this is why Open Graph uses its own `og_title`/`og_description`/`og_image` blocks rather than reusing `title`/`description` a second time. 2026-09-04; verified via `manage.py check`/`makemigrations --check` (clean), and live `runserver`/`curl` checks: `/robots.txt`/`/sitemap.xml` both 200 with correct content, the favicon serving 200, `/projects/?category=Web&page=2` confirmed canonicalizing to the clean `/projects/`, the "Fire Service Website" project detail page confirmed showing its own real title/description/screenshot in its Open Graph tags (not the site default), and the JSON-LD block confirmed present and correct on the home page. **Deployed 2026-09-04** — see the "production deploys are both done" resolved note above.
- [x] [Phase 18, complete and deployed] Built the Public Feedback / Testimonials feature: a locked design-canvas prototype (matching the site's theme, 5-star picker, name + feedback form, testimonial grid) implemented for real, plus the owner-specified admin moderation workflow. New `core.Feedback` model (`name`/`rating`/`feedback`/`status` — pending/live/viewed — `submitted_at`/`viewed_at`); public `/feedback/` page (AJAX-submittable like Contact/Hire Me, via a new `extra_context` parameter on the shared `_handle_lead_form` helper) showing only `status="live"` testimonials plus an average-rating summary; a CSS-only, no-JS 5-star radio picker (this site's first custom form widget of its kind). Admin Hub gained a new "Feedback" moderation queue (`/adminhub/feedback/` — Live/Unpublish/Viewed/Delete actions per card, following the existing Contact/Hire Me `.hub-notice`-placeholder convention instead of reproducing the public form) and a separate Viewed-archive page (`/adminhub/feedback/viewed/`, a literal `<table>`, reached only via a hub-only "View Viewed Feedback →" button) whose entries auto-delete 20 days after being marked Viewed via a lazy delete-on-request sweep (no scheduler/cron infrastructure exists on this project's host). "Feedback" added as the last nav link on both the public site and the hub (desktop bar + drawer), deliberately excluded from the public footer per the owner's explicit correction on the locked design prototype. 2026-09-04; verified end-to-end via Django's test client (`manage.py shell`): anonymous submission → invisible publicly, Pending in the hub → Live publishes it (visible, average rating/count update) → Unpublish reverts it (invisible again) → Live→Viewed directly un-publishes and archives it in one step → a `viewed_at` backdated 21 days is swept (confirmed deleted from the database) on the archive page's next load; invalid submissions show accessible field errors via both the AJAX and non-AJAX (PRG) paths; Contact/Hire Me's existing AJAX/PRG flows re-verified unaffected by the shared helper's extension. `manage.py check`/`makemigrations --check` clean, `npm run build:css` clean. See `PHASES.md`, `DECISIONS.md`, `ARCHITECTURE.md`. **Deployed 2026-09-04** — see the "production deploys are both done" resolved note above. `/feedback/` is deliberately excluded from `/sitemap.xml` and from search indexing (`noindex` meta tag) — owner's explicit choice, not a gap; see the dedicated `DECISIONS.md` entry.
- [x] [Ad hoc, post-Phase 18] Removed the "See My Resume" button from the public Feedback page's header (`templates/pages/feedback.html` reverted to the plain single-column `page-head` shape `blog.html` uses) and restored the star picker's live rating-word readout (Poor/Fair/Good/Great/Excellent, matching the original design-canvas prototype) via a new CSS-only `:has()`-based reveal (`.feedback-rating-row`/`.feedback-rating-words` in `templates/partials/feedback_form.html` and `static_src/css/input.css`) — the first `:has()` usage anywhere in this codebase. 2026-09-04; verified live in-browser: hovering each star shows the correct word in accent color, clicking and moving away leaves it persisted in dim color, and "Select a rating" shows by default. See `DECISIONS.md`, `ARCHITECTURE.md`.
- [x] [Ad hoc, post-Phase 18] Converted the Admin Hub Feedback queue's Live/Unpublish/Viewed/Delete row actions to AJAX — clicking any of the three no longer reloads the page. Extended the site's existing header-based AJAX pattern (`X-Requested-With` negotiation, same view/URL serving a partial or a redirect) to POST-only action endpoints for the first time; card loop extracted to `templates/adminhub/partials/feedback_results.html`, swapped inside `#feedback-results` via one delegated `submit` listener. The Viewed-archive page's own Delete button was left as a plain POST (out of scope for this request). 2026-09-04; verified via Django's test client — AJAX POSTs return the updated fragment (Live badge/Unpublish button, or the row removed after Viewed/Delete), a non-AJAX POST still 302-redirects as before. See `DECISIONS.md`, `ARCHITECTURE.md`.
- [x] [Ad hoc, post-Phase 18] Committed and pushed all of the above (Phase 18's core feature, the resume-button/rating-word follow-ups, and the Admin Hub AJAX conversion) to `origin/main` (commit `78fd9b7`). Since `render.yaml`'s build step runs `python manage.py migrate` on every deploy, this single push both deployed the code to Render and applied every pending migration — including Phase 17's previously-undeployed `0032_local_seo_copy.py` — to Neon in one step. 2026-09-04; confirmed live via `curl` against `https://shahbazkhan.onrender.com` — see the "production deploys are both done" resolved note above.
- [x] [Ad hoc, post-Phase 18] Confirmed the Feedback page is excluded from search indexing, per the owner's explicit request. It was already absent from the sitemap, but that alone doesn't prevent indexing — a page linked from every page's nav (as this one is) is still discoverable by crawlers regardless of the sitemap. Added the actual exclusion mechanism instead: `templates/pages/feedback.html` now overrides `{% block extra_head %}` with `<meta name="robots" content="noindex, follow">` — `follow` so crawling of the rest of the site through this page's links is unaffected. 2026-09-04; verified via Django's test client that `/feedback/` renders the tag and every other page (checked via `/contact/`) does not. See `DECISIONS.md`.
